---
layer: task
topic: matched-abstraction-thinking
references:
  - ../plan/orchestration.md
  - ../approach/charter.md
---

# Baseline Skill Setup Task

## Task Brief

Create the project-local `matched-abstraction-thinking` skill, baseline layer documents, and a validator that proves the docs preserve Approach, Plan, and Task boundaries.

Scope includes `.codex/skills/matched-abstraction-thinking/`, `docs/matched-abstraction/`, `tests/test_validate_layers.py`, and `tasks/todo.md`.

## Acceptance Contract

The task is accepted when the skill has valid frontmatter, docs exist in all three layer directories, invalid layer references are rejected, Approach and Plan docs reject task checklists, Task docs require verification evidence, and project handoff state is current.

## RED Artifact

Validator tests were written before the validator implementation. The first run failed because `.codex/skills/matched-abstraction-thinking/scripts/validate_layers.py` did not exist.

Layer pressure cases were supplied by independent Approach, Plan, and Task agents before the skill content was written.

## Execution Notes

The skill was scaffolded with the official skill creator script through `python3` because the script file was not executable. The abstraction flow diagram was rendered with Diashort at https://diashort.apps.quickable.co/d/9071cb70.

## Verification Evidence

RED evidence:

- `python3 -m unittest tests/test_validate_layers.py` failed before the validator existed with `/usr/bin/python3: can't open file ... validate_layers.py`.
- After tightening frontmatter rules, the expanded tests failed because missing `topic` and `references` still passed.

GREEN evidence:

- `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests/test_validate_layers.py` -> `Ran 10 tests ... OK`.
- `python3 /home/lagz0ne/.codex/skills/.system/skill-creator/scripts/quick_validate.py .codex/skills/matched-abstraction-thinking` -> `Skill is valid!`.
- `python3 .codex/skills/matched-abstraction-thinking/scripts/validate_layers.py docs/matched-abstraction` -> `Layer validation passed: docs/matched-abstraction`.
- `wc -w .codex/skills/matched-abstraction-thinking/SKILL.md` -> `479`.
- placeholder and uncertainty wording scan -> no matches.
- generated bytecode directory scan -> no matches after cleanup.

Forward-test evidence:

- A fresh subagent used the skill on a hypothetical plugin sandbox design and produced separated Approach, Plan, and Task outputs with layer-specific subagent boundaries.
- The forward-test identified the inline-output edge case, so the skill now states how to proceed when file edits are forbidden.

Review-pass evidence:

- A fresh review subagent found that required sections were validated per layer instead of per document, Task evidence could be promissory, nested docs were skipped, the handoff state was stale, and the Plan referenced subagent protection before Approach authorized it.
- The validator now checks every markdown doc under the layer root, rejects misplaced markdown, validates required sections per document, requires concrete command/result Task evidence, and scans nested layer docs.
- Approach now authorizes one subagent per active layer, making the Plan authority chain consistent.

## Wrap-Up Announcement

The final response must state what changed, what commands verified it, and the workspace caveat that this directory is not a git repository.
