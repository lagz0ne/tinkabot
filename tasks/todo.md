# Tinkabot Handoff

## Current State

- Repo: `lagz0ne/tinkabot`, public, branch `main`.
- Remote: `origin git@github.com:lagz0ne/tinkabot.git`.
- Last completed committed baseline before current work: `5dacbfc docs: record v0.1.1 release`.
- Worktree status: pending work includes prior matched-abstraction/C3/demo changes plus the current Tinkalet-driven live patch update; many dirty surfaces predate this turn and remain intentionally unreverted.
- Root role: orchestration only.
- Current implementation lives in `packages/sdk` and `substrate/go`.
- Active/future lanes:
  - `schemas`: canonical JSON Schema and codegen authority.
  - `substrate/go`: Go embedded-NATS/auth/process/Docker-facing substrate.
  - `apps/frontend`: Vite trusted shell.

## Active Goal

Maximize Tinkabot as a NATS-native app substrate per `tasks/tinkabot-nats-native-max-okr.md`: realtime UI, auth, request/reply, KV/Object state, reactions, and demos must use NATS primitives end-to-end, with generated UI sandboxed and app performance good enough to feel native. Current target is `5 / 5` NATS-native acceptance families; current read is `5 / 5` counted after local proof plus independent Codex and Claude noninteractive `VERDICT: PASS`. Continue autonomously and interrupt only for a real unblock question.

## Active Session

Tinkabot NATS-native max OKR — GREEN (2026-06-25). User corrected the architecture/product bar after the chess demo: realtime, auth, request/reply, and KV must use NATS to the maximum, never show non-NATS product paths, and performance must be top notch. Active `/goal` and `tasks/tinkabot-nats-native-max-okr.md` track the `5 / 5` NATS-native product acceptance families: NATS auth boundary, request/reply action path, KV/Object durable truth, reactive state delivery, and product-grade demo performance. CKR-UI-WATCH now uses a viewer-scoped state prefix minted with the browser bearer, a shell-only `participant_watch` command, backend KV `WatchFiltered`, trusted-shell NATS subscription, and leased `tinkabot.state` frame messages; generated content does not receive NATS credentials or delivery subjects. Latest proof `/tmp/tinkabot-chess-demo.XbwrdL/chess-proof.json` passed on Tailscale `http://forge.tail6c789a.ts.net:36681` with `stateDelivery=trusted-shell.nats-watch.push`, `generatedIframePollingCount=0` measured from `trusted-shell.dispatched`, p95/p99 `71ms`, touch/visual pass, and `authorityLeakCount=0`. Verification passed: focused embednats/tinkabot Go tests, frontend tests, native-preview typecheck, frontend build, `bash -n scripts/demo-chess.sh`, `bun run demo:chess`, C3 evals for `c3-202`, `c3-401`, `c3-501`, and `c3 check --include-adr`. Codex first review failed session-scoped state subjects, undercounted latency, self-reported polling, and overclaimed `tinkabot.state` wording; the patch fixed those. Final independent review: Claude noninteractive `VERDICT: PASS`; Codex noninteractive `VERDICT: PASS`, findings none.

Tinkabot real chess app OKR — GREEN / HELD LIVE (2026-06-25). User rejected the current harness-like board link because it did not lead to an obvious real board and asked to make the objective a real two-player chess app. Created active `/goal` and persisted `tasks/tinkabot-real-chess-okr.md`, then implemented Task `docs/matched-abstraction/task/real-chess-app.md`. Added `chess.js@1.4.0`, chess-only shell mode `tb_chess=1`, generated-frame join/name/board-code UI, browser-origin chess moves, board-scoped read enforcement in the trusted shell, and release-shaped `scripts/demo-chess.sh` exposed as `bun run demo:chess`. Proof `/tmp/tinkabot-chess-live.k143Sf/dist/chess-proof.json` passed with route `tailscale`, shell `http://forge.tail6c789a.ts.net:44937`, players Alice/Bob, Fool's Mate terminal `checkmate` with black winner and `Qh4#`, denials for duplicate join, wrong-board read, wrong board, wrong turn, illegal move, duplicate action, and stale revision, `reactionMode: tinkalet.watch.prefix`, watched reducer records for all materialized app actions, 64 chess squares on both pages, no parent generic shell text, stable name typing across the polling interval, `authorityLeakCount: 0`, `platformChessAPIAdditions: 0`, and `legalityEngine: chess.js@1.4.0`. Held live run `/tmp/tinkabot-chess-live.k143Sf/dist` passed the same proof, then created fresh manual board `board-1782376267705-manual` with reducer log `/tmp/tinkabot-chess-live.k143Sf/dist/chess-manual-reducer.log` and manual smoke proof `/tmp/tinkabot-chess-live.k143Sf/dist/chess-manual-smoke.json`; Alice link `http://forge.tail6c789a.ts.net:44937/?tb_app=demo&tb_participant=alice&tb_state=apps.demo.state.chess.board-1782376267705-manual&tb_session=demo-001&tb_chess=1&tb_board_no=board-1782376267705-manual`; Bob link `http://forge.tail6c789a.ts.net:44937/?tb_app=demo&tb_participant=bob&tb_state=apps.demo.state.chess.board-1782376267705-manual&tb_session=demo-001&tb_chess=1&tb_board_no=board-1782376267705-manual`. Manual smoke JSON verified both links render chess-only pages with 64 squares, `waiting` status, preserved typed name values, and no generic shell text. Verification passed: `bash -n scripts/demo-chess.sh`; focused frontend tests; frontend native-preview typecheck; `bun run demo:chess`; held live run; focused `git diff --check`; C3 lookups; C3 evals for `c3-302`, `c3-401`, `c3-501`, and `c3-502`; `c3 check --include-adr`. Independent review closed: Codex initial review failed board-code/manual-smoke/terminal-claim gaps; after fixes, Codex follow-up and Claude follow-up both returned `VERDICT: PASS`; the name-input refresh fix also received Codex and Claude `VERDICT: PASS`.

Anonymous typerace golden app — GREEN (2026-06-25). Added `docs/matched-abstraction/task/anonymous-typerace-golden.md`, `scripts/demo-typerace-anon.sh`, and `bun run demo:typerace`. The generated UI now supports full-screen `tb_type=1` typeracing with anonymous runner aliases, race prompt, progress meters, mistake count, and winner display while the trusted shell keeps the same scoped lease, request/reply, and NATS watch-push path. The demo builds the release archive, starts packaged Tinkabot with two opaque `anon-*` participant profiles, verifies Tailscale shell route `http://forge.tail6c789a.ts.net:42299`, removes the packaged NATS sidecar before user-level commands, opens two browser pages, drives join/progress actions through browser-origin `participant_action`, and runs a Tinkalet reducer over `tinkalet watch prefix`. Proof `/tmp/tinkabot-typerace-demo.kzwmR5/typerace-anon-proof.json` passed with kind `tinkabot.anonymousTypeRaceProof.v1`, winner `anon-1782386213030972302-a`, denials for participant escape, duplicate join, duplicate action, stale revision, and late progress after finish, `reactionMode: tinkalet.watch.prefix`, `stateDelivery: trusted-shell.nats-watch.push`, `generatedIframePollingCount: 0`, p95/p99 `66ms`, `authorityLeakCount: 0`, and `platformTypeRaceAPIAdditions: 0`. Verification passed: `bash -n scripts/demo-typerace-anon.sh`; focused frontend tests; frontend native-preview typecheck; frontend build; `bun run demo:typerace`. Remaining non-claims: no public lobby/signup provisioning, no max participant or max-rate freeze.

Browser turn-based board Task handoff — GREEN (2026-06-25). Added the playable tic-tac-toe-shaped browser reference demo at `scripts/demo-turn-browser.sh` plus `bun run demo:turn-browser`. The demo builds the release archive, starts packaged Tinkabot with `demo:alice` and `demo:bob`, verifies Tailscale shell route `http://forge.tail6c789a.ts.net:33975`, opens two leased browser pages, submits five legal browser-origin moves through `participant_action`, and runs a demo-owned Tinkalet reducer for apply/reject. Proof `/tmp/tinkabot-turn-browser-demo.5jDpfs/browser-turn-board-proof.json` has kind `tinkabot.browserTurnBoardProof.v1`, route `tailscale`, five applied moves, denials for participant escape, wrong turn, duplicate action, stale revision, and occupied cell, synchronized Alice/Bob final board winner `alice`, and `authorityLeakCount: 0`. Frontend board mode is gated by `tb_board=1` and uses only generic `participant_read` / `participant_action`; no Go/platform board API was added. Verification passed: `bash -n scripts/demo-turn-browser.sh`; focused frontend tests; frontend native-preview typecheck; `bun run demo:turn-browser`; `git diff --check`; C3 lookups for the changed script/frontend/doc/handoff surfaces; C3 evals for `c3-302`, `c3-401`, `c3-501`, and `c3-502`; `c3 check --include-adr`.

Tinkabot multitenant isolation DKR-0 — DONE (2026-06-25). Two read-only DKR probes plus orchestrator verification mapped existing bundle, participant, Tinkalet, browser, revocation, and realtime proofs to the new six-scenario objective. Result: `3 / 6` counted. Counted: runtime role separation, same-app user/participant boundary, and generated UI boundary. Partial: tenant/app boundary lacks a positive two-app one-daemon proof; Tinkalet middleman boundary has strong product commands but public docs still show direct NATS config-bucket tuning as a normal-looking path; concurrent operations proof is split across single-app demos and restart tests. Focused current checks passed: targeted Tinkabot authority/browser/realtime/turn tests; bundle/binary profile tests; targeted Tinkalet profile/watch/action/trigger tests; embednats bundle account/revocation tests; frontend isolation tests. Recommended next CKR is `CKR-ISO-AUTH`: one daemon, two app scopes, two users per app, allowed own-scope paths, denied cross-app read/watch/action/direct `$KV`, and targeted revocation with another app still live.

Tinkabot multitenant isolation DKR-1 / CKR-ISO-AUTH — FROZEN (2026-06-25). DKR-1 topology decision: first proof is one daemon with multiple app scopes at the participant/action/material layer; per-app instances remain valid but do not substitute for shared-daemon app isolation, and SaaS control-plane work stays deferred. Frozen Task doc: `docs/matched-abstraction/task/multitenant-two-app-scope.md`. RED gap: no named acceptance proof previously showed two positive app scopes succeeding concurrently in one daemon while also denying cross-app paths.

Tinkabot multitenant isolation CKR-ISO-AUTH — GREEN (2026-06-25). Added `docs/matched-abstraction/task/multitenant-two-app-scope.md` and `substrate/go/tinkabot/multitenant_isolation_test.go`. The proof boots one Tinkabot daemon, admits `demo:alice`, `demo:bob`, `other:alice`, and `other:bob`, imports each through isolated Tinkalet envs, proves own-app item read/action/watch for `apps.demo.*` and `apps.other.*`, denies cross-app item read/watch/action/direct participant-record read/raw action subject/raw `$KV`/Tinkalet bundle trigger/raw `tb.bundle.clock.tick` for all four participants, and revokes `demo:alice` while `demo:bob`, `other:alice`, and `other:bob` stay active. RED exposed a real product gap: participant `tinkalet trigger bundle.clock.tick` returned `connection-failed`; fixed Tinkalet to deny restricted profiles before network as `denied-scope` while preserving `revoked-credentials` for revoked restricted profiles. Independent review loop: initial Codex review failed the proof because `other:bob` and raw trigger denial were asymmetric; the test was strengthened, then final Codex and Claude noninteractive reviews returned `VERDICT: PASS`. Focused verification passed: `go test ./tinkabot -run TestMultitenantTwoAppScopeIsolation -count=1 -v`; Tinkalet restricted trigger/watch/action denial suite; `go test ./tinkabot ./tinkalet -count=1`; focused C3 evals. Objective progress reached `4 / 6` here; the following docs slice then closed the Tinkalet middleman boundary.

Tinkabot multitenant isolation Tinkalet-middleman docs boundary — GREEN (2026-06-25). Public examples now state that normal user, LLM, and transform work goes through Tinkalet profiles and commands; packaged/raw NATS is owner/operator diagnostics and low-level config inspection only. This resolves the public integration-model anti-goal without hiding the lower-level NATS mechanism from operator documentation. Objective progress is now `5 / 6`; remaining partial is the combined concurrent operations proof.

Tinkabot multitenant isolation CKR-ISO-CONCURRENCY — GREEN (2026-06-25). Added `docs/matched-abstraction/task/multitenant-concurrency-reference-demo.md`, `scripts/demo-iso-concurrency.sh`, `bun run demo:iso-concurrency`, and `TestMultitenantConcurrentRestartCatchUp`. The focused regression proves one daemon with `demo:alice`, `demo:bob`, `other:alice`, and `other:bob`, concurrent Tinkalet action submission, own-cursor replay, product and direct raw-NATS cross-scope denials before and after restart, same-store restart, cursor catch-up, duplicate replay timeout, and targeted revocation with the other three participants still live. The packaged demo builds the release archive, exposes initial and restart Tailscale shell URLs, disables the packaged NATS sidecar before user-level commands, drives 24/24 actions through packaged Tinkalet, records zero revision gaps, zero duplicate replay, 48 product denials, zero authority/leak counters, observed per-participant action rate `61.86Hz`, action latency p95 `36ms`, watch replay latency p95 `5046ms`, and `capacity_claim:"observed-only"`; packaged revocation stays unclaimed until an owner-facing revoke command exists. Codex follow-up and Claude follow-up both returned `VERDICT: PASS` with no findings after the raw post-restart denial fix. Objective progress is now `6 / 6`.

Tinkabot multitenant isolation OKR run control — STARTED (2026-06-25). User corrected the product frame: the explicit model is Tinkabot as daemon/server authority, Tinkalet as middleman/profile surface, and NATS auth as the isolation boundary for multiple concurrent isolated users/apps. Created active `/goal` for this objective and persisted the Reverse Tornado board in `tasks/tinkabot-multitenant-isolation-okr.md`. RED: the previous handoff still pointed at the completed app-substrate mission, and multitenancy could be misframed as a later SaaS/control-plane concern. GREEN: the new board defines objective, six isolation scenario families, measurable anti-goal tripwires, deployment interpretation, DKR-first decomposition, CKR candidates, and anti-goal eval points. Next move is DKR-0 evidence inventory, then DKR-1 topology decision before freezing any CKR.

Tinkabot objective OKR run control — COMPLETE (2026-06-24). User ratified the objective and asked for fully autonomous operation: do not pause for "just checking"; interrupt only for a real unblock question. Created active `/goal` for the NATS-native app substrate objective and persisted the Reverse Tornado board in `tasks/tinkabot-objective-okr.md`. RED: the previous handoff had completed Tinkalet/bundle demo proof but no current operating board for the next objective, no DKR-first worker queue, and no direct mission metrics for the visualization and multiplayer families. GREEN: the new board defines the objective metric, mission targets, security/completeness/abstraction/Double-V/NATS-first anti-goal tripwires, action envelope, DKR-to-CKR structure, provisional CKR candidates, three anti-goal eval points, flags, freshness contract, and state storage. DKR-0 through DKR-4 structured CKR-VIS, CKR-AUTH, CKR-TURN, CKR-REALTIME, and CKR-TEACH. CKR-AUTH, CKR-TURN, and CKR-REALTIME are green as generic prerequisites. Read-only DKR audit `019ef94c-41b2-7f20-afaf-4e9996652099` returned `PASS_TO_COUNT` for scoped multiplayer rows M1-M6, so the scoped multiplayer mission is counted. CKR-VIS now adds `item_submit`, scoped `item-watcher` Tinkalet profiles, and `demo:visual`; `/tmp/tinkabot-visual-green3/visual-decision-proof.json` passed with Tailscale route, rendered bundle artifact proof, one accepted submit, zero denied intents/dispatches, zero authority leaks, isolated watcher readback with no owner profile, owner readback, post-restart owner readback, and transform-chain change. Objective progress is now `2 / 2`; C3 coverage/eval/check plus Codex and Claude noninteractive review passed. Max participant/rate, arbitrary browser publisher identity, dynamic arbitrary LLM bundle publication, and live-before-submit watcher timing remain non-claims.

CKR-AUTH RED slice — PREPARED (2026-06-24): tests/docs only. Authority matrix and expected RED failures live in `docs/matched-abstraction/task/participant-authority.md`; Go RED is `substrate/go/tinkabot/participant_authority_test.go`; frontend RED is `apps/frontend/tests/isolation.test.ts`. Expected failures were missing generic participant admit/revoke/profile types in Go and missing app/participant frame lease mediation in frontend isolation.

CKR-AUTH GREEN slice — DONE (2026-06-24): generic participant authority prerequisite, not a complete multiplayer mission. `substrate/go/tinkabot/tinkabot.go` adds `ParticipantProfile`, `AdmitParticipant`, `RevokeParticipant`, participant scoped JWT permissions, local participant descriptors, and NATS-backed `tinkabot.participant.v1` records in `tb_items`. Repeated admit for the same app/participant now rotates the profile by revoking the prior recorded scoped NATS user before accepting the replacement. `substrate/go/tinkalet/tinkalet.go` imports `app-participant` profiles, preserves participant app/id metadata, denies neighbor/revoked/scope failures without leaking credentials or raw subjects, and keeps owner profiles separate. `apps/frontend/src/isolation.ts` binds optional `appId`/`participantId` into frame leases and rejects mismatches as `FrameScopeEscape`. Review loop: Claude returned PASS but flagged softer direct-NATS revocation/read-denial proof; Codex returned FAIL on duplicate admit leaving older scoped creds valid. Follow-up fixed duplicate-admit rotation and strengthened `TestParticipantAuthority` to prove stale rotated creds fail direct NATS reconnect, revoked creds fail direct NATS reconnect, another participant remains live, cross-participant read emits a NATS permission error, and wrong-app/config/schedule raw writes are denied. Final independent review: Codex focused rereview PASS with no findings/gaps; Claude final focused confirmation PASS with no findings/gaps after the read-denial oracle required the async NATS permission error. Proof: `go test ./tinkabot -run TestParticipantAuthority -count=1`; targeted tinkabot authority suite; `go test ./tinkalet -count=1`; `go test ./... -count=1`; `bun test apps/frontend/tests/isolation.test.ts`; `bunx @typescript/native-preview --noEmit -p apps/frontend/tsconfig.json`; `C3X_MODE=agent c3x lookup substrate/go/tinkabot/participant_authority_test.go`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=474`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x check --include-adr` -> `ok: true`. C3 eval bindings now include `participant_authority_test.go` under `c3-201` and `ref-shadow-authority-boundaries`.

CKR-TURN app-action/revision prerequisite — GREEN (2026-06-24): generic action ingress, not a complete turn-based mission. `substrate/go/tinkabot/tinkabot.go` now starts a Tinkabot-owned action service on `tb.app.<app>.participants.<id>.action`; participant identity is derived from the NATS subject, not request JSON. `tinkalet action submit` infers app/participant from the selected `app-participant` profile, sends action id/state key/base revision/value to the product service, and never exposes KV subjects in normal output. Participant credentials can read only scoped app state, their own action records, and their own participant record for sync; mutation authority is limited to the own action request subject. The action service has least-privilege item-bucket grants for app-state direct reads and action-subtree reads/writes, checks state revision before creating an action record, maps duplicate ids to `duplicate-action`, maps stale state to `stale-revision`, rejects malformed subjects and cross-app state keys, and leaves final state mutation to the next reducer/turn-state slice. Tinkalet item reads now preserve the selected profile when mapping NATS permission errors, so participant cross-scope reads report `denied-scope` instead of `connection-failed`. Tests cover allowed scoped reads, denied cross-app reads, denied direct item writes, denied raw `$KV` writes, denied cross-participant action subjects, missing state, stale revision non-materialization, duplicate action ids, malformed subject/token validation, revoked/stale rotated profiles, and Bob remaining live. Proof: `go test ./tinkabot -run 'TestAppActionMalformedSubject|TestParticipantAppActions|TestParticipantAuthority' -count=1`; `go test ./tinkalet -count=1`; `go test ./... -count=1`; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=477`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x check --include-adr` -> `total: 28`, `ok: true`. Mission count remains `0 / 2`; next autonomous move is the generic reducer/turn-state contract that consumes pending action records and resolves shared app state with CAS before any tic-tac-toe or typeracing-specific API.

CKR-TURN reducer/CAS prerequisite — GREEN (2026-06-24): generic reducer command, not a complete turn-based mission. Added `docs/matched-abstraction/task/app-action-reducer-contract.md` and bound it to `c3-302`. `tinkalet action apply <action-key> --value <next-state>` now reads a pending `tinkabot.appAction.v1`, validates the embedded action key/state key/base revision, creates a deterministic pending `<action-key>.receipt` as the apply/reject serialization claim, CAS-updates the shared app state item with NATS KV `Update`, and finalizes the receipt as `resolved` / `applied`. App or bundle logic still owns legality and next-state derivation; the platform still knows nothing about tic-tac-toe cells, turns, or scores. Tests prove successful apply, durable receipt, state update, duplicate receipt denial, stale state denial without receipt materialization when stale is observed before the claim, participant `denied-scope`, and Tinkalet malformed/no-profile apply denials. Focused proof: `go test ./tinkabot -run 'TestAppActionMalformedSubject|TestParticipantAppActions|TestParticipantAppReducer|TestParticipantAuthority' -count=1`; `go test ./tinkalet -count=1`. Mission count remains `0 / 2`; next autonomous move is the release-shaped CKR-TURN acceptance bundle/demo that proves legal completion plus wrong-turn, stale, duplicate, and occupied-cell denial using this generic substrate.

CKR-TURN reference proof — GREEN (2026-06-24): release-shaped turn proof, still not a complete scoped multiplayer mission. Added `docs/matched-abstraction/task/turn-based-reference.md` and bound it to `c3-302`. `cmd/tinkabot --participant <app>:<id>` now admits repeated scoped participant profiles at startup and prints their store dirs. `tinkalet action reject <action-key> --reason <reason-token>` is the generic denial mate to `action apply`: owner/reducer profiles validate pending action shape, scoped state key, receipt absence, and current state revision, then create `<action-key>.receipt` with status `denied`, outcome `rejected`, reason, action revision, and state revision without mutating shared state. `action apply` now claims the deterministic receipt key before state mutation, so apply/reject serialize through NATS KV `Create` rather than racing state against receipt. `TestTurnBasedReferenceMission` and `bun run demo:turn` prove a tic-tac-toe-equivalent sequence through Tinkalet-only product commands: legal completion, wrong-turn rejection, duplicate-action denial, stale-revision non-materialization, and occupied-cell rejection. Latest `bun run demo:turn` built the package, started packaged Tinkabot, exposed Tailscale URL `http://forge.tail6c789a.ts.net:42781`, disabled the packaged NATS sidecar before commands, and wrote proof `/tmp/tinkabot-turn-demo.EIsQmj/turn-proof.json`. Verification: focused cmd/tinkalet/tinkabot tests; `go test ./... -count=1`; `bash -n` over demo/package scripts; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=479`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x eval c3-302`; `C3X_MODE=agent c3x check --include-adr`. Independent follow-up: Codex noninteractive `VERDICT: PASS`, findings none; Claude noninteractive `VERDICT: PASS`, findings none. Residuals are explicit non-claims: submit-time stale materialization can race and be caught by reducer CAS, and crash recovery from a pending receipt needs a future reclaim path. Mission count remains `0 / 2`; next autonomous branch is DKR/CKR-REALTIME participant-rate and browser sync measurement, or CKR-VIS generic visual submit plus scoped LLM watcher.

CKR-REALTIME browser DOM sync prerequisite — GREEN (2026-06-24): measurement-only proof on the existing clock chain, not scoped multiplayer mission completion. Added `docs/matched-abstraction/task/realtime-browser-sync.md`; `scripts/demo-chain-reaction.sh` accepts `TINKABOT_DEMO_BROWSER_SYNC=1` and runs Playwright against the generated clock UI through the shown shell URL; `examples/clock/scripts/present.sh` now timestamps filter receipt before parse work so the reported filter latency is the actual watch-to-filter receipt boundary. Latest proof ran `TINKABOT_DEMO_BROWSER_SYNC=1 TINKABOT_DEMO_FAST_EVERY=100ms bash scripts/demo-chain-reaction.sh /tmp/tinkabot-rt-green.32iCRS`, exposed Tailscale URL `http://forge.tail6c789a.ts.net:40327/artifacts/bundle/clock/index.html`, wrote `/tmp/tinkabot-rt-green.32iCRS/realtime-browser-sync.json`, and passed with browser age p95 `149ms`, p99 `173ms`, filter p95 `19ms`, source interval p95 `140ms`, and `58` unique seqs over `6000ms`. Verification: `sh -n` for clock scripts; `bash -n` for demo/package scripts; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=480`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x eval c3-502`; `C3X_MODE=agent c3x check --include-adr`; Codex noninteractive `VERDICT: PASS`, findings none; Claude noninteractive `VERDICT: PASS`, findings none. Residuals are explicit non-claims: latest-projection polling can coalesce revisions, participant-rate was not measured in this browser proof, terminal materialization was not proven by this browser proof, and max participants/rate is still unfrozen.

CKR-REALTIME participant revision-watch prerequisite — GREEN (2026-06-24): safe sync primitive for scoped participants, not participant-rate or realtime mission completion. Added `docs/matched-abstraction/task/realtime-participant-watch.md`, `TestParticipantRealtimeWatchEnvelope`, `TestParticipantWatchScopeDenialPrecedesNetwork`, and `TestParticipantWatchFiltersDenyMalformedTargets`. `tinkalet watch` keeps existing `WatchAll` behavior for non-participant profiles, but app-participant profiles locally validate target grammar/scope before NATS connection and use server-side `WatchFiltered` only for `apps.<app>.state` or the participant's own `apps.<app>.participants.<id>.actions` subtree. Proof covers ordered app-state revisions, own action plus receipt revision visibility, neighbor action-subtree denial before network, broad consumer-create denial, no malformed-target filter escape, and no credential/raw `$KV`/store leaks. Verification: focused Tinkalet participant watch-denial tests, focused Tinkabot realtime watch/action/authority suite, full Go suite, C3 coverage/eval/check, and Codex plus Claude independent review all passed. Residuals: this slice alone was not high-rate, reconnect/restart, terminal-result, or browser UI proof.

CKR-REALTIME packaged participant reference demo — GREEN (2026-06-24): release-shaped packaged proof, still not realtime-heavy browser UI or scoped multiplayer mission completion by itself. Added `docs/matched-abstraction/task/realtime-participant-reference-demo.md`, `scripts/demo-realtime-participant.sh`, and `bun run demo:realtime`. The demo builds the release archive, starts packaged Tinkabot with Alice/Bob participant profiles, exposes a Tailscale shell URL, disables the packaged NATS sidecar before user-level commands, drives 60 scoped Tinkalet actions at `50.59Hz` per participant, verifies filtered participant watches account for 60/60 actions with zero missing-id/order gaps, materializes terminal state under `apps.demo.state.terminal`, rejects a late action as `race-finished`, and records zero user-level/product-output authority leaks. Latest proof: `/tmp/tinkabot-realtime-demo.zJPQCA/realtime-reference-proof.json`, Tailscale URL `http://forge.tail6c789a.ts.net:39893`. Verification: `bun run demo:realtime`; focused realtime/action/authority Go suite; focused `cmd/tinkabot` participant startup suite; focused Tinkalet participant watch-denial suite; `go test ./... -count=1`; demo/package `bash -n`; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=488`, `lookup_errors=0`, `uncovered=0`; `c3 eval c3-302`; `c3 eval c3-501`; serial `c3 check --include-adr` -> `total: 28`, `ok: true`; Codex initial/follow-up/final redaction reviews `VERDICT: PASS`; Claude initial/follow-up/final redaction reviews `VERDICT: PASS`. Residuals: browser participant action/readback is covered by a separate green prerequisite; this slice alone did not count the scoped multiplayer mission.

CKR-REALTIME browser participant action/readback bridge — GREEN (2026-06-24): trusted-shell-mediated bridge prerequisite, still not realtime-heavy browser UI or scoped multiplayer mission completion by itself. Added `docs/matched-abstraction/task/browser-participant-action-bridge.md` and bound it to `c3-302`. Canonical `browser.command_intent.context` now admits optional `appId` and `participantId`; Tinkabot owns `tb.app.browser.command` for `participant_action` and `participant_read`; `participant_action` validates shell context and payload scope, then forwards creation to the existing `tb.app.<app>.participants.<id>.action` service; `participant_read` returns only `apps.<app>.state.>` or the participant's own action material. The packager rebuilds the embedded frontend before Go binaries so packaged shells do not serve stale mediation code. Raw-authority filtering now aligns the trusted shell, Go bridge, and rebuilt generated shell asset on credential terms including `password`, `bearer`, `cred`, `jwt`, `nkey`, `seed`, and `secret`. Verification: RED schema and Go no-responder failures were captured first; GREEN focused SDK/front-end/Go bridge tests passed; focused realtime/action/authority Go suite passed; `go test ./tinkabot ./cmd/tinkabot ./tinkalet -count=1`; `go test ./... -count=1`; `bun test packages/sdk/tests/base-contract`; narrowed JS proof over contract-authority, command-acceptance, frontend isolation, and observe tests -> `22 pass`; `bun run build:frontend`; `bun run pack:tinkabot -- /tmp/tinkabot-pack-bridge-rawfix`; package/release/realtime-demo `bash -n`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=489`, `lookup_errors=0`, `uncovered=0`; `git diff --check`. Independent review: Codex bridge review `VERDICT: PASS` with trusted-shell-publisher binding recorded as a residual; Claude bridge review `VERDICT: PASS` and flagged raw-key drift; Codex and Claude raw-key follow-ups both returned `VERDICT: PASS` after the fix. Blocked checks are explicit: full frontend browser tests need `/usr/bin/google-chrome`; SDK typecheck is blocked by adjacent local `@lagz0ne/nats-embedded` missing built `dist` types and lacking installed `tsdown`. Residuals: no arbitrary browser publisher identity claim, raw-key matching is exact normalized in frontend/Go while SDK remains stricter, no direct generated UI NATS credential, no max-rate freeze, no realtime-heavy browser UI proof yet; this slice alone did not count the scoped multiplayer mission.

CKR-REALTIME browser participant UI proof — GREEN (2026-06-24): release-shaped browser UI prerequisite, still not full scoped multiplayer mission completion. Added `docs/matched-abstraction/task/realtime-browser-participant-ui.md`, `scripts/demo-realtime-browser.sh`, and `bun run demo:realtime-browser`. The trusted shell now dispatches accepted generated-frame `participant_read` / `participant_action` intents through the cookie-gated browser command NATS connection and returns filtered backend responses to the generated iframe; generated content still receives only a lease and posts typed intents. The demo builds the release archive, starts packaged Tinkabot with `demo:alice` and `demo:bob`, uses `TB_DEMO_SESSION=demo-001` for the cookie-gated browser command credential, exposes the shell through Tailscale, seeds state through packaged Tinkalet, and drives two browser pages. RED caught the Tailscale HTTP-origin crash from `crypto.randomUUID`; `makeLease` now falls back to `crypto.getRandomValues` while keeping nonce generation in the trusted shell. Codex review then found trusted-shell `tb_session` query interpolation, weaker raw-key matching, and missing direct DOM counter assertions; fixes assign `tb_session` as an input property, align frontend/Go raw-key matching with SDK substring behavior, and assert iframe `actions`/`readbacks`/`denied` counters plus a malicious-session injection guard in the demo. Claude review then found rejected command-client caching, accumulating observe connections, narrow leak terms, missing generated-action key guards, and unfiltered outbound command replies; fixes reset failed clients, close stale observe connections, broaden leak terms, guard missing keys, and filter backend responses before iframe delivery. Latest proof: `/tmp/tinkabot-realtime-browser-green5/realtime-browser-proof.json`, Tailscale URL `http://forge.tail6c789a.ts.net:43315`, `browserPages=2`, `acceptedActions=40`, `readbacks=40`, `deniedDispatches=0`, `authorityLeakCount=0`, action p95 `6ms`, action p99 `9ms`, readback p95 `7ms`, readback p99 `10ms`, shell injection guard passed, Alice/Bob DOM `complete` with 20 actions, 20 readbacks, and 0 denials each. Verification: focused frontend isolation/observe tests; frontend typecheck; frontend build; release-shaped browser demo; focused browser command Go test; focused realtime/authority Go suite; package-level Go suite; full `go test ./...`; script syntax checks; `git diff --check`; C3 coverage `owned_files=491`, `lookup_errors=0`, `uncovered=0`; focused C3 evals and `c3 check --include-adr` passed. Independent review: Codex final review `VERDICT: PASS`, findings none; Claude final review `VERDICT: PASS`, zero new security findings, confirmed all eight prior findings closed. Residuals: trusted-shell-mediated prerequisite only, not arbitrary browser identity proof and not full scoped multiplayer mission completion.

CKR-VIS visual decision loop — GREEN (2026-06-24): LLM visualization mission proof. Added `docs/matched-abstraction/task/visual-decision-loop.md`, `scripts/demo-visual-decision.sh`, and `bun run demo:visual`. Tinkabot now accepts `item_submit` on `tb.app.browser.command` for `artifacts.<artifact>.results.*`, stores choices as guarded `tb_items` records with browser-command provenance, and denies duplicate, stale, out-of-scope, malformed, and raw-authority payloads. `cmd/tinkabot --watcher <name>:<item|prefix>:<target>` mints scoped `item-watcher` profiles; Tinkalet imports them as read-only watcher profiles that can watch exact scope, cannot direct `item get`, cannot broaden, and deny after revocation. Latest proof: `/tmp/tinkabot-visual-green3/visual-decision-proof.json`, Tailscale URL `http://forge.tail6c789a.ts.net:35995`, rendered clock bundle artifact, one accepted visual submit, zero denied intents/dispatches, zero authority leaks, isolated watcher event `choice=diagram-a` with no owner profile in the watcher Tinkalet environment, owner readback, post-restart owner readback, and `transformChanged=true`. Verification: focused visual Go tests; full `go test ./... -count=1`; frontend isolation/observe tests; native frontend typecheck; frontend build; release-shaped visual demo; script syntax checks; `git diff --check`; C3 coverage `owned_files=494`, `lookup_errors=0`, `uncovered=0`; `c3x eval c3-302`, `c3x eval c3-501`, `c3x eval c3-502`, and `c3x check --include-adr`; Codex final review `VERDICT: PASS`; Claude final review `VERDICT: PASS`. Residuals: no Mermaid-specific platform API, no raw browser NATS credential, no owner-profile-as-LLM-watch happy path, no dynamic arbitrary LLM bundle publication claim, watcher event is replay rather than live-before-submit, and leak terms are conservative.

CKR-REALTIME participant action-gap harness — GREEN (2026-06-24): bounded two-participant action/sync measurement, not max-rate, 60s capacity, reconnect, browser UI, or terminal-scoring proof. Added `docs/matched-abstraction/task/realtime-participant-action-gap.md` and `TestParticipantRealtimeActionGapHarness`. The harness admits Alice and Bob, creates independent NATS-backed app state keys, starts each participant's own filtered KV watch, submits 24 unique action ids per participant at a 25ms cadence through scoped NATS request/reply, and fails if any expected action id is missing, duplicated, out of scope, or observed with non-increasing per-participant KV revisions. This proves the generic action ingress plus participant watch path can account for expected vs observed action revisions under a short CI-rate load without adding a game-specific API or custom realtime channel. Verification: `go test ./tinkabot -run TestParticipantRealtimeActionGapHarness -count=1`; focused action/watch/authority suite; `go test ./tinkalet -run 'TestParticipantWatchScopeDenialPrecedesNetwork|TestParticipantWatchFiltersDenyMalformedTargets' -count=1`; `go test ./... -count=1`; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=482`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x eval c3-302` includes `realtime-participant-action-gap.md`; `C3X_MODE=agent c3x check --include-adr` -> `total: 28`, `ok: true`; Codex noninteractive `VERDICT: PASS`, findings none; Claude noninteractive `VERDICT: PASS`, no claim mismatches. Residuals: no sustained/breakpoint claim beyond the separate 60s baseline proof, no reconnect/restart proof in this slice, no terminal-result proof in this slice, and no generated/browser UI proof yet.

CKR-REALTIME sustained participant-rate proof — GREEN (2026-06-24): 60s baseline only, not max-rate, reconnect, browser UI, or terminal-scoring proof. Added `docs/matched-abstraction/task/realtime-participant-sustained-rate.md` and `TestParticipantRealtimeSustainedActionGapHarness`. The long proof reuses the generic action-gap harness with Alice and Bob submitting 600 unique action ids each at a 100ms cadence through scoped NATS request/reply while their own filtered KV watches observe every expected id. The watcher cleanup now closes the participant connection instead of granting broad KV consumer-delete authority. Verification: `go test ./tinkabot -run TestParticipantRealtimeActionGapHarness -count=1 -v`; `TINKABOT_REALTIME_SUSTAINED=1 go test ./tinkabot -run TestParticipantRealtimeSustainedActionGapHarness -count=1 -timeout 2m -v` -> `1200` actions across `2` participants in `59.906036488s`; focused action/watch/authority suite; `go test ./... -count=1`; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=483`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x eval c3-302` includes `realtime-participant-sustained-rate.md`; `C3X_MODE=agent c3x check --include-adr` -> `total: 28`, `ok: true`; Codex noninteractive `VERDICT: PASS`, findings none after independently rerunning the sustained proof and gates; Claude noninteractive first pass `VERDICT: FAIL` only because independent-review evidence was not yet recorded, while runtime/authority/anti-overclaim/C3 claims passed; Claude follow-up `VERDICT: PASS`, evidence gap closed with no new overclaim. Residuals: no max participant/rate freeze, no reconnect/restart proof in this slice, no terminal-result proof in this slice, and no generated/browser UI proof yet.

CKR-REALTIME reconnect/restart proof — GREEN (2026-06-24): cursor catch-up prerequisite only, not terminal scoring, browser UI, max-rate, or mission completion. Added `docs/matched-abstraction/task/realtime-participant-reconnect-restart.md`, `TestParticipantRealtimeReconnectRestartCatchUp`, and `TestOperatorRevocationAfterRestart`. RED first failed at compile time on the new acceptance helpers; after helpers, runtime RED failed with `action rt-alice-1 denied submit: connection-failed`, exposing stale participant descriptors after restart. GREEN: active participant descriptors refresh to the restarted NATS/shell endpoint, Alice re-imports the persisted participant profile without re-admit, the same Tinkalet cursor replays exactly missed retained action records after restart, duplicate cursor replay times out, and persisted root-signed participant JWTs remain revokable after runtime restart. Verification: `go test ./tinkabot -run TestParticipantRealtimeReconnectRestartCatchUp -count=1 -v`; `go test ./embednats -run TestOperatorRevocationAfterRestart -count=1 -v`; focused tinkabot realtime/action/authority suite; focused embednats revocation suite; focused tinkalet participant watch-denial suite; `go test ./... -count=1`; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=484`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x eval c3-302` includes `realtime-participant-reconnect-restart.md`; Codex noninteractive `VERDICT: PASS`, findings none; Claude noninteractive `VERDICT: PASS`, findings none after the harmless test cleanup was removed. Residuals: no terminal-result proof in this slice; no generated/browser UI proof yet; no max participant/rate freeze.

CKR-REALTIME terminal-result materialization proof — GREEN (2026-06-24): generic terminal materialization prerequisite only, not browser UI, typeracing API, max-rate, or mission completion by itself. Added `docs/matched-abstraction/task/realtime-participant-terminal-result.md` and `TestParticipantRealtimeTerminalResultMaterialization`. RED first failed at compile time on missing terminal proof helpers. GREEN: Alice and Bob submit terminal actions through scoped participant profiles; owner/reducer applies legal actions through `tinkalet action apply`; Bob's late action after terminal state is rejected through `tinkalet action reject`; final result state is durable `tb_items` material under `apps.demo.state.terminal`; scoped participant state watch observes the final state revision; each participant's own action watch accounts final receipts, including the denied late-action receipt; no platform scoring service, terminal-specific subject, broad watch, or custom realtime channel was added. Verification: focused terminal proof; focused realtime/action/authority suite; focused Tinkalet watch-denial suite; `go test ./... -count=1`; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> `owned_files=485`, `lookup_errors=0`, `uncovered=0`; `C3X_MODE=agent c3x eval c3-302` includes `realtime-participant-terminal-result.md`; `C3X_MODE=agent c3x check --include-adr` -> `total: 28`, `ok: true`; Codex noninteractive `VERDICT: PASS`, findings none, no blocking gaps after rerunning focused terminal/watch/C3 checks; Claude noninteractive `VERDICT: PASS`, findings none, gaps none. Residuals: this slice alone did not include realtime-heavy participant UI/reference proof; no max participant/rate freeze.

Tinkalet-driven live patch model — DONE (2026-06-23). User clarified that the live patch demo should model user integration through Tinkalet instead of direct NATS CLI use, and that the README should explain the chain setup, Tinkalet setup, and use viewpoints. Task-layer scope: generalize `tinkalet trigger` from the single `bundle.clock.tick` tour to a safe `bundle.<bundle>.<entry>` product intent grammar, move `demo:patch` to import/use the emitted profile and trigger `bundle.builder.source` through packaged Tinkalet with the NATS sidecar disabled, and update README/example docs to teach the three-viewpoint flow. RED: focused Tinkalet test for `bundle.builder.source` initially failed with `unknown-trigger`; malformed overlong intent remains denied before network use. GREEN: `subjectFor` now maps only valid `bundle.<bundle>.<entry>` tokens to hidden `tb.bundle.<bundle>.<entry>` subjects; `scripts/demo-live-patch.sh` imports/uses the emitted profile, renames the packaged NATS sidecar to `nats.disabled`, and has Playwright trigger `bundle.builder.source` through packaged Tinkalet; README and builder README explain chain setup, Tinkalet setup, and use. Also fixed stale `TestBundle/AppServes` text after the current clock Mermaid UI changed from `tinkabot clock` to `tinkabot sequence`. Verified: `go test ./tinkalet -count=1`; `go test ./tinkabot -run TestBundle -count=1`; `go test ./... -count=1` from `substrate/go`; `bash -n scripts/demo-live-patch.sh scripts/demo-chain-reaction.sh scripts/release-package.sh scripts/package-tinkabot.sh`; `bun run demo:patch` passed with Tailscale URL `http://forge.tail6c789a.ts.net:35491/artifacts/bundle/builder/index.html`, browser proof `artifact app.rev.1 -> artifact app.rev.2`, trigger proof `profile local accepted bundle.builder.source`, and package sidecar absent except `nats.disabled`; `bun run release:evidence`; `scripts/c3-line-coverage-harness.sh` -> 471 owned files, 0 lookup errors, 0 uncovered; `C3X_MODE=agent c3x check --include-adr`; `git diff --check`.

Review follow-up for Tinkalet-driven live patch model — DONE (2026-06-23). Claude re-review passed after the package/source README clarification. Codex re-review first found two low issues: Tinkalet's product grammar allowed uppercase/underscore while Tinkabot bundle tokens are lowercase/digit/hyphen, and `tasks/todo.md` still named stale baseline `a45463f`; both are fixed. Added `TestTriggerIntentGrammarDenials` for uppercase, underscore, and wildcard intents. Final Codex narrow follow-up passed: baseline is `5dacbfc`, grammar rejects anything outside lowercase letters, digits, and hyphen. Final rerun after fixes: `go test ./tinkalet -count=1`; `go test ./... -count=1` from `substrate/go`; `bash -n scripts/demo-live-patch.sh scripts/demo-chain-reaction.sh scripts/release-package.sh scripts/package-tinkabot.sh`; `bun run demo:patch` passed at `http://forge.tail6c789a.ts.net:46109/artifacts/bundle/builder/index.html` with trigger `profile local accepted bundle.builder.source` and sidecar `nats.disabled`; `bun run release:evidence`; `scripts/c3-line-coverage-harness.sh` -> 471 owned files, 0 lookup errors, 0 uncovered; `C3X_MODE=agent c3x check --include-adr`; `git diff --check`.

Builder live patch demo — DONE (2026-06-23). User clarified that the demo should patch running work and let the browser-facing app update through the reaction chain, like Vite, instead of creating a fresh service or requiring a manual reload. Task-layer scope: keep the existing builder bundle shape, add a generated browser watcher for the scoped built projection, add a package-shaped `demo:patch` harness that starts packaged Tinkabot once, triggers `tb.bundle.builder.source` over NATS, and verifies the served artifact/projection advance plus already-open browser refresh. RED: standalone builder source output did not contain `_p/built` or `location.reload`/`location.replace`; first package harness run failed usefully because it assumed `assets/main.js` while real Vite output was `assets/index.js`; Codex review found that the first green harness proved projection/artifact advance but not browser refresh. GREEN: `examples/builder/scripts/source.sh` now emits millisecond timestamped source with a browser `_p/built` watcher that `location.replace`s to the new `artifactRevision`; `scripts/demo-live-patch.sh` starts the release-shaped builder package, exposes a Tailscale URL, waits for the cold built artifact, opens Chromium on that URL, triggers `tb.bundle.builder.source` through the bundled NATS CLI, and proves the loaded page refreshes while `bundle.builder.built` plus the served JS artifact advance in place; `package.json` exposes it as `bun run demo:patch`; README and builder README describe the live patch flow. Verified: `sh -n examples/builder/scripts/source.sh`; standalone framed source output parses with byte-length check and includes `_p/built` plus `location.replace`; `bash -n scripts/demo-live-patch.sh scripts/demo-chain-reaction.sh scripts/release-package.sh scripts/package-tinkabot.sh`; `bun run demo:patch` passed with Tailscale URL `http://forge.tail6c789a.ts.net:33357/artifacts/bundle/builder/index.html`, browser proof `artifact app.rev.1 -> artifact app.rev.2`, built projection `app.rev.1 -> app.rev.2`, changed JS artifact, and browser proof JSON showing `trigger:"accepted"` plus URL `?rev=app.rev.2`; `git diff --check`; `scripts/c3-line-coverage-harness.sh` -> 471 owned files, 0 lookup errors, 0 uncovered; `C3X_MODE=agent c3x check --include-adr`; `bun run release:evidence`; Claude re-review -> `VERDICT: PASS`; Codex re-review -> `VERDICT: PASS`; held demo session remains at `http://forge.tail6c789a.ts.net:34719/artifacts/bundle/builder/index.html`.

Tinkabot self-sequence artifact — DONE (2026-06-23). User asked to express the sequence diagram through Tinkabot itself and then allowed CDN dependencies. Updated the clock bundle artifact emitted by `examples/clock/scripts/tick.sh` so the generated page loads Mermaid `11.15.0` from jsDelivr, renders the Tinkabot/Tinkalet/NATS/bundle/shell/UI sequence diagram, and keeps the live `_p/view` timing probe below it. Fixed the probe to unwrap the projection envelope (`env.value || env`) so observed update count and browser age are based on the actual materialized view. Hardened the shell artifact frame by exporting `LC_ALL=C` for byte-counted `Content-Length` and JSON-escaping the page body before embedding it. Updated `examples/clock/README.md` to document that the sequence is Mermaid loaded from a CDN inside generated bundle UI. Verified: `sh -n examples/clock/scripts/tick.sh examples/clock/scripts/present.sh`; standalone `tick.sh` framed output parses as two JSON effects with byte-length checks and includes exact Mermaid CDN version, encoded sequence source, projection unwrap logic, and `live projection`; `git diff --check -- examples/clock/scripts/tick.sh examples/clock/README.md tasks/todo.md`; separate `C3X_MODE=agent c3x lookup` checks for `examples/clock/scripts/tick.sh`, `examples/clock/README.md`, and `tasks/todo.md`; `scripts/c3-line-coverage-harness.sh` -> 470 owned files, 0 lookup errors, 0 uncovered; jsDelivr module `HEAD` for Mermaid `11.15.0` returns 200 with CORS and immutable cache headers; `bunx playwright install chromium` installed the local Playwright browser used for render proof; `bun run demo:chain` passed and held tmux session `tinkabot-showoff-mermaid-104943` open at `http://forge.tail6c789a.ts.net:36579/artifacts/bundle/clock/index.html`; public artifact fetch confirmed Mermaid CDN/source/unwrap text and CSP `sandbox allow-scripts`; public `_p/view` advanced from seq `1782211814043` to `1782211814446` with `sourceIntervalMs` 105 and `filterLatencyMs` 15; Chromium render against the Tailscale URL confirmed nonblank Mermaid SVG (`svgChildren` 127), live projection panel, `observedUpdates` 7, `browserAgeMs` 25, `sourceIntervalMs` 104, and `filterLatencyMs` 18.

Realtime sync probe — DONE (2026-06-23). User asked to push the app realtime to test the sync limit. Kept the clock manifest default at 5s so the release smoke still proves manual trigger against the conservative app, but changed `examples/clock/scripts/tick.sh` to emit millisecond `ms`/`seq` values and changed `present.sh` to expose `sourceIntervalMs`, `filterReceivedMs`, and `filterLatencyMs` through `bundle.clock.view`. The clock artifact page now samples `_p/view` with one browser fetch in flight and displays browser age/fetch timing. `bun run demo:chain` retunes `bundle.clock.tick.every` to the existing 100ms runtime floor through `config_bucket` before disabling the packaged NATS CLI sidecar for Tinkalet commands; `TINKABOT_DEMO_FAST_EVERY=manifest` skips the override. Verified: `sh -n examples/clock/scripts/tick.sh examples/clock/scripts/present.sh`; `bash -n scripts/demo-chain-reaction.sh scripts/smoke-tinkalet-package.sh scripts/release-package.sh scripts/package-tinkabot.sh`; standalone framed output from both clock scripts; `bun run demo:chain` passed with Tailscale URL `http://forge.tail6c789a.ts.net:36071/artifacts/bundle/clock/index.html`; held tmux session `tinkabot-showoff-rt` passed and kept `http://forge.tail6c789a.ts.net:37321/artifacts/bundle/clock/index.html` live; Tailscale `_p/view` advanced from seq `1782205770450` to `1782205770571` with `sourceIntervalMs` around `89`/`121` and `filterLatencyMs` around `11`/`14`; `bun run smoke:tinkalet-package`; `bun run release:evidence`; `scripts/c3-line-coverage-harness.sh` -> 470 owned files, 0 lookup errors, 0 uncovered; `git diff --check`; Claude full-patch noninteractive review -> `VERDICT: PASS`; Codex full-patch noninteractive review -> `VERDICT: PASS` after a first Codex rerun correctly flagged an incomplete review packet that omitted the untracked demo script. No C3 parent delta needed: this is a Task-layer demo/example timing probe using existing bundle schedule config semantics.

README chain-reaction concept/demo — DONE (2026-06-23). User asked for a concept explanation, sequence diagram, staple demo script, and README coverage. Added `README.md` concept + Mermaid sequence for the Tinkabot/Tinkalet/bundle/reaction model, added `scripts/demo-chain-reaction.sh`, and exposed it as `bun run demo:chain`. The demo builds the release-shaped package, starts packaged `tinkabot` with the clock bundle, imports/selects the local profile, removes the packaged NATS CLI sidecar before Tinkalet commands, triggers the clock and observes projection change, registers a local transformer reaction, resolves the watched source item, verifies transformed item writeback, sets a server-owned schedule, verifies scheduled item writeback, and turns the schedule off. Follow-up: user made this the favorite showoff path and required Tailscale URLs because local addresses are not accessible from their side; the demo now keeps Tinkabot's loopback shell posture, opens a short-lived Tailscale forwarder for the shell URL, prints/verifies the MagicDNS URL when available, uses loopback for internal assertions, and supports `TINKABOT_DEMO_HOLD=1` to keep the shell open for teaching. Verified: `bun run demo:chain` passed with Tailscale URL `http://forge.tail6c789a.ts.net:37641/artifacts/bundle/clock/index.html`; held `tmux` showoff session `tinkabot-showoff` passed and kept `http://forge.tail6c789a.ts.net:41159/artifacts/bundle/clock/index.html` live; `curl -fsS` against that Tailscale URL returned the clock HTML; `bash -n scripts/demo-chain-reaction.sh scripts/smoke-tinkalet-package.sh scripts/release-package.sh scripts/package-tinkabot.sh`; `C3X_MODE=agent c3x lookup scripts/demo-chain-reaction.sh` -> `c3-501`; `scripts/c3-line-coverage-harness.sh` -> 470 owned files, 0 lookup errors, 0 uncovered; `git diff --check`.

C3 onboarding and line-coverage OKR — DONE (2026-06-23). User asked to onboard C3 and frame a Reverse Tornado OKR for line-by-line code-to-doc coverage, with anti-goals of no missing code, no workaround, and no single LLM truth. Initialized `.c3`, applied genesis ADR `adr-00000000-c3-adoption`, and created first-rung containers/components/refs/rules covering contracts/SDK, Go runtime, Tinkalet edge, browser shell, proof/docs/workflow, NATS-native chain reactions, bundle-as-app behavior, shadow authority, coverage ratchet, and sandbox/raw-authority rules. Added `.c3/eval` specs for all created facts, `tasks/c3-line-coverage-okr.md`, and the durable `scripts/c3-line-coverage-harness.sh`. Fixed Codex-found harness gaps where the first extension allowlist missed tracked proof/config surfaces such as `--`, `.gitignore`, `CLAUDE.md`, and `tools/natscli/*`, and where lookup command errors were not reported separately. The current harness uses current `git ls-files` plus non-ignored untracked files, excludes only dependency/build/generated/cache classes and `.c3/c3.db`, and now verifies 470 owned files with 0 lookup errors and 0 uncovered after the README demo script landed. Initial C3 closure verified locally: `c3 change apply adr-00000000-c3-adoption`; `c3 check --include-adr`; `c3 eval` -> 21 holds, 0 drift, 0 needs-judgement; `scripts/c3-line-coverage-harness.sh` -> 469 owned files, 0 lookup errors, 0 uncovered. Final Codex noninteractive YOLO review passed for that closure: `VERDICT: PASS`, `FINDINGS: none`, `GAPS: none`; independent replay found 0 lookup command errors and 0 `matches[0]`, and the only existing git-visible file outside the owned set was allowed `.c3/c3.db`. Final Claude noninteractive YOLO review also passed for that closure: `VERDICT: PASS`, `GAPS: none`; it independently replayed all 469 lookups, reconciled 475 git-visible paths to 469 current owned paths plus 5 deleted tracked paths and allowed `.c3/c3.db`, and confirmed no-match lookups exit 0 but are correctly detected through `matches[0]`. Anti-goals were satisfied for that round: no missing owned files, no coverage workaround, and dual Codex/Claude review artifacts exist.

Artifact cleanup — DONE (2026-06-22). User asked to apply all findings from the matched-abstraction supplementary artifact review. Removed stale `charter.md` Approach references from `endgame-app`, `platform-structure`, `browser-frontend-mediator`, and `nats-script-runtime`; extended `docs/manual/v1.md` and `release/v1.json` so `gate:tinkalet-package` cites the schedule set -> item get -> schedule off transcript already exercised by the package smoke; and added the missing operational artifact pack to `docs/matched-abstraction/task/tinkalet-release-docs-and-proof.md`. Verified: stale `charter.md` reference grep found no active docs hits; `bun run release:evidence`; `bun test tests/release-evidence.test.ts`; `git diff --check`. Runtime smoke checks are currently blocked in this environment because Tinkabot exits before posture with `EmbeddedNATSAdapter.ServerStartFailed: embedded NATS server did not become ready`; reproduced with `bun run gate:manual`, `bun run gate:tinkalet-package`, direct `go run ./cmd/tinkabot --store <tmp> --shell 127.0.0.1:0`, and `go test ./tinkabot -run TestBinaryFirstStartMaterializes -count=1` from `substrate/go`.

Matched-abstraction cleanup — DONE (2026-06-18). User clarified that the project should rely on the dedicated `matched-abstraction` skill, not a hand-rolled local layer process or checker. Removed the project-owned checker script, removed the layer-check package scripts, removed those commands from the Codex orchestrator and quality-slice workflow, deleted the old bootstrap docs (`approach/charter.md`, `plan/orchestration.md`, `task/baseline-skill-setup.md`), deleted the transient checker-focused onboarding Task doc, and removed obsolete Python layer-check evidence lines from historical Task docs. The skill remains installed as the canonical `.agents/skills/matched-abstraction` package with `.claude/skills/matched-abstraction` as a compatibility symlink. Verified: active command-surface grep found no layer-check scripts; `bun test tests/codex-orchestrator.test.ts`; `bun run typecheck:orchestrator`; `claude plugin validate --strict .agents/skills/matched-abstraction`; `git diff --check`.

Tinkabot v0.1.1 GitHub Release — DONE (2026-06-17). Published `https://github.com/lagz0ne/tinkabot/releases/tag/v0.1.1` after the schedule/package proof. Chose a patch release instead of rewriting the existing `v0.1.0` release because `v0.1.0` already had downloads and pointed to an older commit. Bumped root and SDK package versions to `0.1.1`, extended package smoke to cover schedules, fixed README install snippets to point at `v0.1.1`, pushed `main`, rebuilt the release archive from clean commit `a45463f`, verified the packaged README contains only `v0.1.1` install paths, recreated `v0.1.1` after an initial asset was built before the README snippet fix, and uploaded `tinkabot-v0.1.1-linux-amd64.tar.gz` plus `.sha256`. Verification: `bun run gate:tinkalet-package`; `bun run release:package /tmp/tinkabot-release-v0.1.1`; `cd /tmp/tinkabot-release-v0.1.1 && sha256sum -c tinkabot-v0.1.1-linux-amd64.tar.gz.sha256`; packaged README grep for `v0.1.1`; `gh release view v0.1.1 --json tagName,targetCommitish,isDraft,isPrerelease,name,publishedAt,url,assets`; `git rev-parse --short v0.1.1` == `a45463f`; `git status --branch --short` clean. Current release assets report zero downloads immediately after recreation; tag target and local HEAD both resolve to `a45463f`.

Tinkalet schedule package proof — DONE (2026-06-17). Follow-up after committing Slice 5 as `fd93acb feat: add tinkalet schedules`. Extended `scripts/smoke-tinkalet-package.sh` so `bun run gate:tinkalet-package` now builds the release archive, starts packaged `tinkabot`, removes the packaged NATS sidecar before Tinkalet commands, imports/selects the local profile, triggers the clock, sets a product schedule with packaged `tinkalet schedule set`, observes the scheduled item through packaged `tinkalet item get --json`, and cancels it with `schedule off`. Updated README/manual/example wording so Tinkalet schedules are discoverable while bundle manifest cadence remains documented as a bundle-specific config override. Updated the Slice 5 Task evidence to include the package gate. Verified: `bash -n scripts/smoke-tinkalet-package.sh scripts/release-package.sh scripts/package-tinkabot.sh`; `bun run gate:tinkalet-package`; `bun run gate:manual`; `bun run release:evidence`; `bun test tests/release-evidence.test.ts`; `git diff --check`. Next recommended move: push `main`; optional next implementation slice is release-manifest schedule milestone/gate bookkeeping if we want release evidence to name schedules explicitly before tagging.

Tinkalet schedules Slice 5 — DONE (2026-06-17). Implemented `docs/matched-abstraction/task/tinkalet-schedules.md`: `tinkalet schedule set <name> --every <duration> --write <item-key> [--value <json>] [--json]` writes durable `tinkabot.schedule.v1` records; `tinkalet schedule off <name> [--json]` cancels them; and Tinkabot now owns a narrow scheduler principal plus server-side interval loop that writes `tinkabot.item.v1` ticks with `tinkabot-schedule` provenance. A focused subagent review tightened the outside-in proof: the main schedule test uses a restricted profile that can edit schedules and read the result item but cannot write `tb_items`, proves the item appears without a Tinkalet daemon, verifies restart catch-up advances sequence and item revision without resetting, verifies `schedule off` remains stable across another restart, and checks schedule outputs for raw KV/credential leakage. The implementation split the scheduler credential from the materializer credential after the first GREEN run exposed a NATS control-line limit when schedule permissions were added to the existing service JWT. Docs/examples now distinguish Tinkalet product item schedules from bundle manifest cadence overrides. Verified: `go test ./tinkalet -run TestScheduleCommandDenials -count=1`; `go test ./tinkabot -run TestTinkaletSchedules -count=1`; `go test ./cmd/tinkalet ./tinkalet -count=1`; `go test ./tinkabot -run 'TestTinkaletSchedules|TestTinkaletItemRecords|TestTinkaletItemWatchUsesWatchStream|TestTinkaletDaemonWatchCursorRestartCatchesRetainedEvents|TestTinkaletLocalReaction|TestTinkaletTriggerClock|TestBinaryRestartReloadsWithoutRegeneration|TestBinaryFirstStartMaterializes' -count=1`; `go test ./... -count=1` from `substrate/go`; `bun run gate:scenarios`; `bun run gate:manual`; `bun run release:evidence`; `bun test tests/gate-checkers.test.ts`; `bun test tests/release-evidence.test.ts`; `git diff --check`. Next recommended move: release consolidation/package smoke and publication decision, with calendar syntax, time zones, multi-item fan-out, retry policies, rich logs, and multi-node scheduler leadership deferred.

Tinkalet local-reactions Slice 4 — DONE (2026-06-17). Implemented `docs/matched-abstraction/task/tinkalet-local-reactions.md`: `tinkalet reaction add` registers one explicit item/status reaction under `TINKALET_DATA_DIR/reactions/<name>.json` mode `0600`, and `tinkalet daemon react <name> --once` watches through the stored profile/cursor, executes an explicit local argv with a scrubbed environment, writes stdout/stderr/exitCode as a resolved product item, and advances the reaction cursor only after successful writeback. A focused subagent review tightened the tests: the main outside-in proof uses a reactor-only profile that can watch and write its result item but cannot direct-read items; command execution proves literal argv handling and no inherited `SECRET_CREDS`/`TINKALET_DATA_DIR`; registry and cursor files are mode/privacy checked; command failure and denied writeback do not advance the matching-event cursor and rerun on the next daemon invocation; removed profile denies before running the command. Verified: `go test ./tinkalet -run TestReactionCommandDenials -count=1`; `go test ./tinkabot -run 'TestTinkaletLocalReaction|TestTinkaletReactionFailureKeepsCursor|TestTinkaletReactionDeniedWritebackKeepsCursor|TestTinkaletReactionRemovedProfileDoesNotRun' -count=1`; `go test ./tinkabot -run 'TestTinkaletLocalReaction|TestTinkaletReactionFailureKeepsCursor|TestTinkaletReactionDeniedWritebackKeepsCursor|TestTinkaletReactionRemovedProfileDoesNotRun|TestTinkaletItemWatchUsesWatchStream|TestTinkaletDaemonWatchCursorRestartCatchesRetainedEvents|TestTinkaletItemRecords' -count=1`; `go test ./cmd/tinkalet ./tinkalet -count=1`; `go test ./... -count=1` from `substrate/go`; `bun run gate:scenarios`; `bun run release:evidence`; `bun test tests/gate-checkers.test.ts`; `git diff --check`. Next recommended move: Slice 5 `schedules`, keeping retry budgets, rich reaction logs, sandboxing, and long-running daemon supervision deferred.

Tinkalet watch-cursors Slice 3 — DONE (2026-06-17). Implemented `docs/matched-abstraction/task/tinkalet-watch-cursors.md`: `tinkalet watch item|prefix`, `tinkalet daemon watch item|prefix`, cursor files under `TINKALET_DATA_DIR/cursors` mode `0600`, newline-delimited JSON or human item-event output, cursor scoping to profile/source/scope/target, duplicate/stale revision skip, future cursor denial as `stale-cursor`, live watch output, and daemon-style cursor restart catch-up. Tinkabot now creates `tb_items` with `nats.KeyValueMaxHistory` so same-key offline updates are retained for cursor replay. A focused subagent review tightened the test plan: outside-in tests use a custom watch-only profile that lacks direct item get/read authority, prove two ordered same-key live revisions, prove daemon restart catches two retained offline revisions without replaying the seed, assert cursor file mode, and keep raw KV/credential details out of output. Verified: `go test ./tinkalet -run TestWatchCommandDenials -count=1`; `go test ./tinkabot -run 'TestTinkaletItemWatchUsesWatchStream|TestTinkaletDaemonWatchCursorRestartCatchesRetainedEvents' -count=1`; `go test ./tinkabot -run 'TestTinkaletItemWatchUsesWatchStream|TestTinkaletDaemonWatchCursorRestartCatchesRetainedEvents|TestTinkaletItemRecords|TestTinkaletTriggerClock|TestTinkaletAuthorityDenials' -count=1`; `go test ./cmd/tinkalet ./tinkalet -count=1`; `go test ./... -count=1` from `substrate/go`; `bun run gate:scenarios`; `bun run release:evidence`; `bun test tests/gate-checkers.test.ts`; `git diff --check`. Next recommended move: Slice 4 `local-reactions`, keeping command execution/writeback/retry behavior out of the watch layer.

Tinkalet item-records Slice 2 — DONE (2026-06-17). Implemented `docs/matched-abstraction/task/tinkalet-item-records.md`: `tinkalet item create/get/resolve/wait`, JSON and one-line human output, malformed/no-profile denials, duplicate create, stale revision, wait-to-resolved, privacy checks that keep raw KV/credential details out of output, and restart recovery over real embedded NATS. Tinkabot now materializes `tb_items`, grants caller-profile item KV authority, and exposes item records through the selected Tinkalet profile without requiring the raw NATS CLI. The restart proof exposed that app-plane JetStream state was keyed by an ephemeral `TB_APP` account; fixed by persisting built-in `TB_APP`/`TB_CONTROL` account keys and built-in account revocations while keeping runtime-minted bundle accounts ephemeral. `TestBinaryRestartReloadsWithoutRegeneration` now also proves pre-stop caller creds stay denied after restart and new process creds connect. Verified: `go test ./tinkalet -run TestItemCommandDenials -count=1`; `go test ./tinkabot -run TestTinkaletItemRecords -count=1`; `go test ./tinkabot -run 'TestTinkaletItemRecords|TestBinaryRestartReloadsWithoutRegeneration|TestLocalProfileDescriptor|TestTinkaletTriggerClock|TestTinkaletAuthorityDenials' -count=1`; `go test ./cmd/tinkalet ./tinkalet -count=1`; `go test ./... -count=1` from `substrate/go`; `bun run gate:scenarios`; `bun run release:evidence`; `bun test tests/gate-checkers.test.ts`; `git diff --check`. Next recommended move: Slice 3 `watch-cursors`, with real KV watch/cursor recovery and daemon restart coverage before local reactions.

Tinkalet profile-trigger Slice 1 — DONE (2026-06-17). Implemented the refined first Tinkalet tour from Approach -> Plan -> umbrella Task -> bounded sub-Tasks. New artifacts: `docs/matched-abstraction/approach/tinkalet-edge.md`, `docs/matched-abstraction/plan/tinkalet-edge.md`, `docs/matched-abstraction/task/profile-trigger-tour.md`, `tinkalet-cli-profile.md`, `tinkalet-local-profile-source.md`, `tinkalet-trigger-live.md`, `tinkalet-authority-denials.md`, `tinkalet-package-tour.md`, and `tinkalet-scenario-gate.md`. Code: `substrate/go/cmd/tinkalet` plus `substrate/go/tinkalet` implement help/version/profile import/list/use/defaults, managed caller credential copies under `TINKALET_DATA_DIR`, explicit config/data dirs, selected-profile trigger for `bundle.clock.tick`, `nats.go` request/reply with request-id forwarding, accepted/duplicate JSON and text output, local-source denied-neighbor, revoked/stale/malformed denials, and privacy-preserving output. Tinkabot now emits `<store>/local-profile.json`. Packaging now builds `tinkalet` beside `tinkabot`, declares both commands in `release.json`, and has `bun run smoke:tinkalet-package`, which starts packaged `tinkabot`, imports/selects the local profile with packaged `tinkalet`, removes the packaged NATS sidecar, triggers the clock, and observes projection change. README/examples now present Tinkalet as the clock trigger path and leave raw NATS for diagnostics/schedule controls. `gate:scenarios` now requires `tinkalet-trigger` and the matrix cites all seven pinned families. Verified: `go test ./cmd/tinkalet ./tinkalet -count=1`; `go test ./tinkalet -run TestTriggerMalformedResponse -count=1`; `go test ./tinkabot -run 'TestLocalProfileDescriptor|TestTinkaletTriggerClock|TestTinkaletAuthorityDenials' -count=1`; `go test ./... -count=1` from `substrate/go`; `bun run smoke:tinkalet-package`; `bun run gate:scenarios`; `bun test tests/gate-checkers.test.ts`; `bun run release:evidence`; `bash -n scripts/package-tinkabot.sh scripts/release-package.sh scripts/smoke-tinkalet-package.sh`; `git diff --check`; touched-file trailing-whitespace scan. Next recommended move: either start Plan Slice 2 `item-records` or do the release-docs-and-proof slice that promotes Tinkalet into the manual/release evidence; do not claim Tinkalet as the full public surface until that promotion lands.

Tinkalet release-docs-and-proof — DONE (2026-06-17). Follow-up after committing Slice 1 as `5f7cd8c feat: add tinkalet profile trigger tour`. Added `docs/matched-abstraction/task/tinkalet-release-docs-and-proof.md`; promoted the package-root Tinkalet profile/trigger tour into `docs/manual/v1.md`; added `gate:tinkalet-package` as a package script backed by `scripts/smoke-tinkalet-package.sh`; made the smoke emit `gate:tinkalet-package passed`; extended `scripts/release-evidence.ts` so required gates include `gate:tinkalet-package` and any gate with manual/verbatim evidence must cite commands present in the manual; updated `tests/release-evidence.test.ts` with the package-gate manual-divergence detector; added a Tinkalet gate result and Tinkalet doc-authority entry to `release/v1.json`. Verified: `bun run gate:tinkalet-package` -> package archive built, packaged `tinkabot` started, packaged `tinkalet` imported/selected local profile, packaged NATS sidecar renamed before trigger, clock projection changed, `gate:tinkalet-package passed`; `bun run gate:manual` -> `gate:manual passed`; `bun test tests/release-evidence.test.ts` -> 30 pass; `bun run release:evidence` -> `release evidence check passed: 17 milestones over 12 spine steps, 6 gate results`; `bun run gate:scenarios`; `bun test tests/gate-checkers.test.ts`; `go test ./... -count=1` from `substrate/go`; `bash -n scripts/package-tinkabot.sh scripts/release-package.sh scripts/smoke-tinkalet-package.sh`; `git diff --check`. Remaining scope: Tinkalet still only owns the Slice 1 profile-trigger tour; schedule control remains diagnostic NATS until the schedule slice, and package publication is still deferred.

Tinkalet CLI profile sub-Task — DONE (2026-06-17). User said "let's go implement this" after the refined plan. Stayed inside the first bounded sub-Task from `profile-trigger-tour`: added `docs/matched-abstraction/task/tinkalet-cli-profile.md`; RED tests in `substrate/go/cmd/tinkalet/main_test.go` and `substrate/go/tinkalet/tinkalet_test.go` failed on missing `run`, `Run`, and `Config`; implemented `substrate/go/cmd/tinkalet` and `substrate/go/tinkalet`. The new CLI supports help/version/usage, `profile import local --store <dir> --name <name>`, `profile list`, `profile list --json`, `profile use <name>`, explicit config/data-dir env roots, managed credential copy at `profiles/<name>/caller.creds` mode `0600`, no writes to `HOME`/`XDG_CONFIG_HOME`/`XDG_STATE_HOME` when explicit dirs are provided, product-shaped import/use/profile-not-found errors, and a trigger profile-selection stub with `--profile`, `--request-id`, `--json`, and `unknown-trigger`/`connection-failed` denials. Verified: `go test ./cmd/tinkalet ./tinkalet -count=1`; `go build -o /tmp/tinkalet-cli-profile ./cmd/tinkalet`; `go test ./... -count=1` from `substrate/go`; `git diff --check`. Not done by design: Tinkabot descriptor emission, live trigger request/reply, packaging archive smoke, scenario gate, README/example promotion.

Profile trigger convergence review — DONE (2026-06-17). User asked not to stop until the plan was refined through a couple more loops. Ran two more focused subagent review rounds after the first triage-three pass. Final blockers were all in the testing contract, not architecture: profile data-dir ownership was ambiguous, no-default `profile list --json` was missing, and the umbrella Task still looked too easy to implement as one giant coding Task. Patched `docs/matched-abstraction/task/profile-trigger-tour.md` so local import uses a managed `profiles/<name>/caller.creds` copy under `TINKALET_DATA_DIR`, profile JSON/human outputs are exact for no-profile/no-default/default states, `unknown-trigger` is a real reason code, package smoke proves Tinkalet does not shell out to the bundled NATS CLI sidecar, and `## Acceptance Contract` explicitly says it is umbrella acceptance only. Patched `docs/matched-abstraction/plan/tinkalet-edge.md` so README/example changes must be clearly labeled Slice 1 clock-tour blocks. Final focused reviewer verdict: NO BLOCKER; `tinkalet-cli-profile` RED tests can start. Verified: `git diff --check`.

Profile trigger testing review — DONE (2026-06-17). User asked for subagent review because weak use-case testing would make implementation pointless. Ran triage-three with three Pusher angles (operator tour, authority/failure, release/package/docs), three Challenger passes, and Arbiter synthesis. Result: 25 proposed findings, all Challenger-confirmed, merged into 8 blocking test-plan gaps plus 2 scope clarifications. Patched `docs/matched-abstraction/plan/tinkalet-edge.md` and `docs/matched-abstraction/task/profile-trigger-tour.md`: Slice 1 now requires a package-root operator transcript, machine-readable local profile source, visible clock state change, deterministic `--request-id` duplicate proof, revoked creds, denied-neighbor/profile isolation, no fallback to stronger creds, profile storage/privacy assertions, strict raw-output leakage oracles, release-archive smoke with scrubbed `PATH`, executable README/example smoke, packaged executable metadata, and scenario/release-gate coverage. Scope clarification: schedule controls stay diagnostic NATS commands until the schedule slice; manual/centralized release-evidence consolidation remains owned by release-docs-and-proof before any release claim says Tinkalet is the primary public surface. Verified: `git diff --check`.

Profile trigger tour Task artifact — RED/READY (2026-06-17). User said "let's do" after the layer-first Plan. Filled the `profile-trigger-tour` artifact pack and added `docs/matched-abstraction/task/profile-trigger-tour.md` with user-flow, profile import/use sequence, trigger authority diagram, state/data map, command contract, failure matrix, test-intent map, acceptance contract, RED artifact, execution notes, scope, and residual risks. RED evidence executed from `substrate/go`: `go test ./cmd/tinkalet -count=1` fails because `cmd/tinkalet` does not exist; `go run ./cmd/tinkalet --help` fails with the same missing directory; `go test ./cmd/tinkabot -run TestRunPrintsVersion -count=1` passes, proving the existing binary remains present while the Tinkalet surface is absent. Verified after doc: `git diff --check`. Next move: write implementation RED tests for `cmd/tinkalet`, profile storage, real embedded-NATS trigger, and product-shaped denial output.

Tinkalet edge Plan — DONE (2026-06-17). User asked to apply matched-abstraction before moving down: each abstraction layer must collect enough artifacts (ASCII diagrams, maps, command sketches, failure matrices, test intent) before descending into design, implementation, or testing Tasks. Added `docs/matched-abstraction/plan/tinkalet-edge.md` with an abstraction descent contract, artifact ladder, product surface map, state/authority map, CLI grammar sketch, six-slice decomposition (`profile-trigger-tour`, `item-records`, `watch-cursors`, `local-reactions`, `schedules`, `release-docs-and-proof`), per-slice required artifact packs, dependency ordering, handoff contract, verification strategy, and escalation log. Verified: `git diff --check`. Next recommended move: fill the `profile-trigger-tour` artifact pack, then create the Task doc with RED evidence before code.

Tinkalet edge Approach — DONE (2026-06-17). User reframed Tinkabot as the server daemon and Tinkalet as the edge client/daemon, then asked for refined, research-informed, practical design with ASCII visuals. Added `docs/matched-abstraction/approach/tinkalet-edge.md` with the Tinkabot/Tinkalet role split, profile/data-dir boundary, item/wait/watch/reaction/schedule vocabulary, server timing vs edge execution boundary, ASCII system/profile/lifecycle/use-case/trust diagrams, non-goals, invariants, decision hierarchy, and Plan-readiness gate. Research anchors used: Kubernetes/Docker context-style profiles, Temporal durable workflow/client/worker split, and NATS JetStream KV watch/history as hidden substrate. Verified: `git diff --check`. Next recommended move: create a Tinkalet Plan whose first release-shaped outcome replaces README NATS hand-holding with Tinkalet profile, trigger, wait, watch, and schedule commands.

Public MIT transition — DONE (2026-06-17). User asked to turn the project public and put it under MIT. Added root `LICENSE` with MIT terms, set package metadata license fields to `MIT` for the workspace, SDK, and frontend package manifests, changed README install docs from private/authenticated `gh release download` to public GitHub Release `curl` downloads, added a README License section, and updated the release package script so future archives include `LICENSE`. Verification: `git diff --check`; `bun run release:package /tmp/tinkabot-license-package` builds and the archive contains `tinkabot-v0.1.0-linux-amd64/LICENSE`; project docs no longer say the repo is private outside dependency docs under ignored `node_modules`.

Package-first release distribution — DONE (2026-06-17). User decided to deliver the package first and defer the npm wrapper. Task-layer scope: keep `pack:tinkabot` as the local unpacked package, add a GitHub-Release-shaped archive, keep npm out of the critical path, and make the package self-contained enough for the README tour. RED: `bun run release:package` failed because no script existed; `go test ./cmd/tinkabot -run TestRunPrintsVersion -count=1` failed because `--version` was undefined; first archive build leaked builder `node_modules` and the release script called a non-executable packer directly. GREEN: `cmd/tinkabot` has ldflags-backed `--version`; `scripts/package-tinkabot.sh` builds `tinkabot`, bundled `bwrap`, and the pinned NATS CLI sidecar at `libexec/tinkabot/nats`; `scripts/release-package.sh` creates `tinkabot-v0.1.0-linux-amd64.tar.gz` plus `.sha256`, release metadata, README, examples, manual, and release evidence; dirty worktree builds identify as `<commit>-dirty`; README/examples prefer packaged sidecars and keep source-checkout fallback commands. Verified: archive contents exclude `node_modules`, checksum validates, `release.json` names bundled sidecars, `./libexec/tinkabot/nats --version` reports `v0.3.0`, unpacked release package serves the clock bundle and bundled NATS request returns `accepted`; direct `bun run pack:tinkabot /tmp/tb-package-direct` includes working `tinkabot`, `nats`, and `bwrap`, `bun run release:evidence`, `bun run gate:manual`, `bun test tests/gate-checkers.test.ts`, `bun run typecheck:orchestrator`, `go test ./... -count=1` from `substrate/go`, and `git diff --check` pass.

Release README/examples prep — DONE (2026-06-17). User asked to prepare for release with the most important surface being README and examples. Task-layer scope: make a fresh evaluator understand the product shape, build a local package, run the clock bundle, drive it through the repo-local NATS CLI tool, and know where the advanced builder example fits. Updated root README to lead with the generated-automation authority promise, release-shaped package command, bundled Bubblewrap lookup/fail-closed posture, repo-local NATS CLI usage, proven properties, checks, examples, repo map, and current limits. Added `examples/README.md`; rewrote `examples/clock/README.md` around source/package runs, manual trigger, schedule controls, and bundle anatomy; rewrote `examples/builder/README.md` around setup, source run, source poke, build projection, and anatomy. Live README smoke: `bun run pack:tinkabot /tmp/tinkabot-release-readme`, packaged binary with `--bundle examples/clock`, page served at `/artifacts/bundle/clock/index.html`, pinned NATS CLI request to `tb.bundle.clock.tick` returned `accepted`. Live builder smoke from the same package: `examples/builder` page served, `bundle.builder.built` existed, source request returned `accepted`, and build projection advanced to `app.rev.2`. Verified after docs: `bun run release:evidence`, `git diff --check`, `bun run gate:manual`, `go test ./... -count=1` from `substrate/go`, `bash -n scripts/package-tinkabot.sh`, `bun test tests/gate-checkers.test.ts`, `bun run typecheck:orchestrator`.

Bundled bwrap sidecar — DONE (2026-06-16). User decided to bundle `bwrap` into the app so sandbox availability is product-shaped instead of host-PATH-only, then tightened the Go-ecosystem testing posture to avoid global CLI requirements by default. Task-layer scope: no setuid helper, no claim that every host can sandbox; package a rootless Bubblewrap sidecar, discover it before PATH, keep `TB_BWRAP` override, keep fail-closed preflight, and make default Go tests hermetic to global `nats` CLI absence without tangling the substrate module with the CLI graph. RED: `go test ./tinkabot -run 'TestResolveBwrap|TestBundledBwrapFailsClosed' -count=1` failed on missing `executable` resolver seam; later `go test ./... -count=1` failed because CLI proof tests expected a global `nats` binary. GREEN: resolver checks `TB_BWRAP` -> executable-adjacent `libexec/tinkabot/bwrap` (including symlink target dir) -> PATH; `scripts/package-tinkabot.sh` builds `tinkabot` and copies executable `bwrap`; `package.json` adds `pack:tinkabot`; manual documents current binary and sidecar layout; `TestBinaryManual` now uses direct `nats.go` clients and named local KV/Object handles after a be-lazy cleanup; `tools/natscli` pins `github.com/nats-io/natscli/nats` as a separate Go tool module and `gate:manual` plus embednats CLI proofs run that tool instead of a global binary. Verified: `go test ./... -count=1` from `substrate/go` passes without global `nats`; `go test ./tinkabot -run 'TestBinaryManual$|TestResolveBwrap|TestBundledBwrapFailsClosed' -count=1 -v`; targeted CLI-proof family runs through the pinned Go tool; `bun run gate:manual` passes without global `nats`; prior sandbox checks passed (`go test ./cmd/tinkabot -count=1`, targeted bwrap tests, `bash -n scripts/package-tinkabot.sh`, `bun run pack:tinkabot /tmp/tb-package-smoke`, `bun run release:evidence`, `git diff --check`).

Product success review follow-up — DONE (2026-06-16). Review of `ce92aa5` found three product-contract/handoff issues: the denial-proof bar asked for a bundle subject-collision case that bundle-v1 makes unrepresentable; generated-app durability/update language conflicted with the process-ephemeral bundle plane; and the friction budget overstated NATS installation even though the binary manages embedded NATS by default. Follow-up edits preserve the sealed technical decisions and reframe unmet items as product gaps or explicit product-claim limitations, not retroactive implementation claims. Verified: `git diff --check -- docs/matched-abstraction/approach/product-success.md tasks/todo.md`. Untracked duplicate skill dir `.agents/skills/matched-abstraction/` remains untouched.

Product success reinforcement — DONE (2026-06-15). User asked to use subagents to reinforce the product-first success contract and make it even better. Ran triage-three-style subagent review across market/adoption severity, operator trust/retention, and buyer value/category sharpness, with Challenger and Arbiter passes to reject bloat. Strengthened `docs/matched-abstraction/approach/product-success.md`: P0 wedge = governed runtime for generated automation that needs real authority; primary enemy = raw scripts/local agents with shared credentials; added beachhead ICP/non-ICP, canonical paid jobs, first paid-worthy win, trust decision-quality bar, real-risk denial proof, policy/authority authoring, recovery/reconciliation honesty, split generated-app vs steerable-session success, behavioral pull metrics, validation protocol, friction budgets, buyer-readable proof, paid-pilot minimum, and scale-expansion bar. Updated diagram: https://diashort.apps.quickable.co/d/537a1569. Verified: `git diff --check -- docs/matched-abstraction/approach/product-success.md tasks/todo.md`.

Production success definition — DONE (2026-06-15). User corrected the framing: do not worry about implementation; be harsh, demanding, and detailed about success so the final product has a chance. Rewrote `docs/matched-abstraction/approach/product-success.md` as a product-first success contract: target user, product promise, minimum product bar, demanding success metrics, what must feel excellent, market success bar, retention bar, trust bar, harsh non-success, and P1 expansion bar. Core claim: Tinkabot only wins if a serious technical operator repeatedly chooses it to run unsafe automation as useful software, with durable proof and control, without handing authority to generated code. C3 check: no `.c3/` directory is present. Verified: `git diff --check -- docs/matched-abstraction/approach/product-success.md tasks/todo.md`.

Bundle sandbox tiers — DONE (2026-06-15). Extracted the bundle sandbox behind a `Sandbox` interface and added a trusted (unsandboxed) opt-in tier.
- `embednats/sandbox.go`: `Sandbox{Preflight,Wrap(SandboxSpec),Name}` + `SandboxSpec`. `BwrapSandbox` (default, fail-closed; Preflight runs the smoke jail) folds the old `sandboxCommand` runtime argv AND `installDeps`' install argv into ONE `Wrap` — they differ only by `BundleWritable` (ro vs rw bundle bind) + `Network` (--unshare-all vs --unshare-pid/--uts). `TrustedSandbox` runs bare. Old `sandboxCommand`/`preflightSandbox` removed.
- Runtime argv is byte-identical to before (security tests stay green). Install no longer uses `--clearenv`/`--dir jailHome`; instead `Wrap`'s `--tmpfs $HOME` is the writable bun HOME and `installDeps` sets a scrubbed `cmd.Env` (PATH+HOME only).
- Selection: `Config.BundleSandbox` / `--bundle-sandbox` ("" = bwrap fail-closed, "none" = trusted + LOUD stderr warning, else typed BundleRejected). Built in `App.buildSandbox`; threaded via `LocalScriptRunner.Bundle` / `FilterLoop.WithSandbox(sb, cfg)`.
- VERIFIED: build, vet (embednats/tinkabot/cmd), `go test ./... -count=1` all green; TestBundleTrustedSandboxTier PASS (boots with TB_BWRAP=/nonexistent + sandbox=none), TestBundleSandboxFailClosed still rejects, TestBundleInstallIgnoresScripts (real bwrap+bun) green. Did NOT run the vite builder (orchestrator live-verifies).

Bundle-sandbox security-review LOOP — CONVERGED (2026-06-15, user: "fix those all, /loop on security review till no issues"). 3 rounds of adversarial subagent review → RED-per-finding → fix → re-review. Commits `6052905` (round 1), `d825446` (round 2), `0e71cd2` (docs).
- Round 1 closed 3 HIGH: symlink path-artifact exfil (os.OpenInRoot no-follow read), operator-key/creds readable in jail (--tmpfs store dir), unsandboxed install RCE (jail bun install: net shared, $HOME+store masked, --ignore-scripts, timeout). + correctness: path-read OOM cap (32MiB), reject `_p/` + `..` artifact names at the gate.
- Round 2 caught the round-1 store mask was asymmetric — RUNTIME jail still left $HOME (~/.ssh) readable via --ro-bind / / (install jail already masked it). Fix: runtime jail --tmpfs $HOME + re-bind only the $PATH dirs under $HOME (devbox/nix toolchain lives there) so sh/coreutils/bun survive while secrets stay masked. Pinned by TestBundleSandboxHidesHomeSecrets.
- Round 3 convergence: NO HIGH/MED actionable issues; all 3 catastrophic threats (read substrate key, reach another bundle, escape jail) closed + live-verified both jails; PATH re-bind held vs adversarial PATH values + through-symlink; residuals R1/R2/R3 accepted-under-posture (see lessons.md).
- New security tests (all RED-first): TestBundle{NoSymlinkArtifactLeak, SandboxHidesStoreSecrets, SandboxHidesHomeSecrets, InstallIgnoresScripts, ArtifactETagRevalidates} + TestBundleSandboxFailClosed + core/embednats unit tests. Full suite + all gates green. LIVE-verified: clock (coreutils) + builder (bun/vite) both build+serve+rebuild under the masked-$HOME jail.
- NOTE: bwrap arg ORDER is load-bearing (masks before binds under them); verify sandbox changes with a LIVE toolchain run, not just sh tests (the sh suite passed while a broken install jail failed the real builder). examples/builder/node_modules is wiped (gitignored; install-at-load reinstalls on next run).

substrate-fit reference resolution (slice D) — DONE (2026-06-15, `2894905` emit-side + `88295c1` serve-side). User's "last missing piece": the substrate owns references both directions (mirror of derive-by-construction) — new Approach invariant 5. A bundle declares only LOCAL refs; the substrate resolves to the derived global namespace at its boundaries:
- EMIT (gate): `ScriptPolicy.ProjectionPrefix`; a script emits short projection id (`view`) / relative artifact name (`page.html`) and the gate prefixes to `bundle.<name>.view` / `bundle/<name>/page.html` (backward-compatible `!HasPrefix` guard; empty-prefix wired slot = no-op). `Allow` now takes a pointer and resolves in place; ScriptLoop + FilterLoop both apply the resolved effect.
- SERVE (fixed convention, user decision): `GET /artifacts/bundle/<name>/_p/<short>` → projection `bundle.<name>.<short>` (in serveArtifact); page fetches relative `_p/view`; Vite base `./` so built assets are `./assets/...` (relocatable, no bundle name baked in).
- examples/clock + examples/builder MIGRATED to pure local refs; proven LIVE: clock page fetches `_p/view`→resolved; builder relative-base build renders (`built from source emitted at <ts>`). Tests: TestBundle/{RelativeRefsResolved,ScopedProjectionRoute}. Full suite + all gates green. Lessons appended.
- substrate-fit program now COMPLETE: slices A (file-referenced artifacts), B (bwrap sandbox + install-at-load), C (ETag/304), D (reference resolution). String-template injection stays the documented fallback (never needed).

Prior: substrate-fit program — DONE (2026-06-15, committed `74a4f63`; Approach+Plan at `docs/matched-abstraction/{approach,plan}/substrate-fit.md`). Folds the Vite-filter issues into the plan by the matched-abstraction move: most issues are author-side (the platform stays ignorant of Vite/esbuild), one principle becomes an Approach invariant — *right primitive per job; never overload one* — and three substrate slices land (order-free, all done):
- **Slice A file-referenced artifacts**: artifact effect may carry a filesystem `path` under the run's `$TB_ARTIFACT_OUT` instead of inline `body`; the runner reads it (confined, no `../`), so large built files bypass the 256 KiB frame ceiling. Script stays NATS-blind; materializer stays the gate. (core ScriptEffect.Path; embednats LocalScriptRunner.Run + FilterLoop.start create per-run outDir + resolve.)
- **Slice B bwrap sandbox (bundles only)**: opt-in `SandboxConfig` on the shared runner/filter (nil = wired slot unjailed); spawn-time bwrap wrap (`--ro-bind / /`, re-`--ro-bind <bundleDir>` AFTER `--tmpfs /tmp` because tmpfs masks /tmp-resident dirs, `--bind <outDir>` writable, `--unshare-all` = no net). Fail-closed preflight (`TB_BWRAP` override or PATH + smoke → BundleRejected). Install-at-load: `bun install` runs UNJAILED before sealing (user decision: install-at-load). `embednats/sandbox.go` new.
- **Slice C content-addressed serving**: serveArtifact sets `ETag` = Object Store sha256 digest, honors `If-None-Match`→304, `no-store`→`no-cache`.
- Proven: TestBundle/{LargeArtifactByPath,ArtifactETag,SandboxBlocksHostWrite} + TestBundleSandboxFailClosed (RED→GREEN each), full suite + ALL gates green, and **examples/builder rebuilding Vite LIVE inside the jail** (~27ms warm, poke→hue change, zero violations/network). Built delegated (3 subagents: A, C parallel; B after; Fable wrote REDs, vetted diffs, ran the live jailed builder). Lessons appended (right-primitive, bwrap/tmpfs masking, install-at-load split, gate:serial annotation). Live binary was on :8424 (kill before reuse).

Prior: Vite reaction-chain example + artifact-overwrite fix — DONE (2026-06-15, committed `6cc0ab1` fix + `e305157` example; resumed under Opus 4.8 after a subagent weekly-limit stall). Asked "what if we do programmatic Vite to do the transform" — proven and shipped. `examples/builder`: a `source` script emits a tiny app's file map as `bundle.builder.src`; a long-lived filter entry (`watches: src`, command `bun`) drives Vite's programmatic `build()` and emits the built HTML/JS as artifact effects under `bundle/builder/`. Poke `tb.bundle.builder.source` over NATS → app rebuilds itself (~15ms warm) → browser reload shows the new build. Proven live end-to-end (agent-browser: `built from source emitted at <ts>` advanced across a poke). BUG FOUND LIVE + FIXED: stable Vite chunk names mean every rebuild overwrites the same artifact name; NATS object-store `Put` purges the prior object's chunks and `return info,err` on denied purge, so the materializer failed `ArtifactWriteFailed` on every overwrite (racy: new meta sometimes published anyway). Fix = narrow `$JS.API.STREAM.PURGE.OBJ_<artifactBucket>` grant in BOTH `servicePerms` (app) and `bundleServicePerms` (bundle), preserving the no-ledger/script-purge boundary. Pinned by deterministic `TestMaterializerArtifactOverwrite` (RED under real restricted perms). DELEGATION NOTE: the builder example was authored by a subagent (it can only run standalone smoke); the full-binary integration run + bug diagnosis + fix were the orchestrator's — that live run is exactly where the perms gap surfaced. node_modules gitignored; `cd examples/builder && bun install` required before running. Lessons appended. ran the other 9 gates green. Live binary was on :8423 (kill before reuse).

Matched-abstraction dedicated skill repo + local replacement — DONE (2026-06-12): canonical skill lives at `/home/lagz0ne/dev/matched-abstraction` and is pushed public at `https://github.com/lagz0ne/matched-abstraction` (`main`, commit `11a6fa7`). Tinkabot consumes the installed project skill `matched-abstraction`; later cleanup removed the local layer-check command surface entirely. The dedicated skill is the authority, not a package script. Caveat: the local `skills` CLI does not support the shorthand `skills lagz0ne/matched-abstraction` without `add`; it returns `Unknown command`.

`bundle-transform` — DONE (2026-06-12, bundle-v1 slice 4/5, user direction: chain-reaction — "frontend may not benefit from consuming kv directly... transform pipe... put that into another kv", refined to "watch the kv and transform as it comes" = LONG-LIVED filter, backend-like). Manifest entries gain `"watches": "<projection short id>"`: a filter process spawned once with the bundle, fed each watched-projection change as one JSON line on stdin (session-steer precedent), emitting framed effects on stdout through rtm.Allow + materializer into its own granted projections. Platform keeps the watching: `SourceRouter.KV` over the bundle account's material bucket (revision cursors, dedupe, stale, LoopSuppressed backstop). Loop safety = load-time DAG (self-watch/cycle/unknown-watch/boot-or-every-combos/no-output all typed BundleRejected); chain/hop inheritance DEFERRED (nothing derives child chains yet — noted in Plan escalation). New embednats surface: `FilterLoop` (filter_loop.go: lazy respawn, process-group kill, no ClaimRun), `RouterResult.Payload` (KV route carries entry.Value()), `core.ScriptRuntime.Allow` exported. Built DELEGATED per efficient-fable (3 subagents: primitive, wiring, example; Fable pinned contract + RED + vetted diffs). Proven: TestFilterLoop 3 families, TestBundle all families incl. TransformPipe (derived view follows watched projection, value actually transformed) + 6 TransformRejected denials; full battery PASS (the skill-extraction caveat above is resolved — evidence is no longer promissory); live browser: page consumes ONLY bundle.clock.view ("display" field created by the filter), advancing on the 5s cadence with zero triggers. examples/clock has the full chain (tick -> state -> present filter -> view -> page). Evidence: `docs/matched-abstraction/task/bundle-transform.md`. Next: slice 5 `bundle-zip`.

Serving-TTL defect — FIXED (2026-06-12, user-found by leaving the binary running): all assembly-internal mints carried `time.Hour` TTLs; nats-server enforces JWT expiry on live connections, so at minute 60 the binary's own plane died (502 artifact/projection routes, ticker stopped, role creds files expired). Fix: `servingTTL = 30 days` const for the five assembly mint sites (roles, router, materializer, bundle service/router); pinned by `TestServingCredsOutliveTheHour` (decodes caller.creds JWT expiry, requires > 24h — RED before fix). DEBT flagged in lessons.md: session-subsystem mints (`StartSessionRuntime` runner, `MintTrustedWrapper`) still carry 1h TTLs — same class, audit when sessions next open. Viewer 10m TTL is intentional (renewal path).

Prior: `bundle-schedule` — DONE (2026-06-12, bundle-v1 slice 3/4, user direction: "automated one should be intended, then control can be done via nats as well (like settings)"). Manifest entries gain `"every": "<dur>"` (100ms floor): the binary fires the entry's trigger on a ticker through the SAME caller request/reply path as boot — every tick an ordinary attributed activation (`tick-<nonce>-<entry>-<n>`), script stays one-shot, materializer stays the gate. Runtime control = NATS settings: config-bucket key `bundle.<bundle>.<entry>.every` under EXISTING caller authority (duration retunes, `off` pauses, delete -> manifest default; consulted once per cycle). Explicitly NOT the deferred HA schedule source (deferral stands). Proven: `TestBundle/ScheduledTicks` (auto-advance with zero manual triggers, settings-off freeze, 200ms resume) + typed MalformedEvery/OverEagerEvery; full battery PASS; live browser: clock self-ticked 5s cadence, `nats kv put ... off` froze it, `1s` resumed. examples/clock now self-ticking. Evidence: `docs/matched-abstraction/task/bundle-schedule.md`. Next: slice 4 `bundle-zip`.

Prior: `bundle-account-isolation` — DONE (2026-06-12, bundle-v1 slice 2/3, user decision: "we play within the boundary of nats auth... same subject doesn't mean a lot as long as imports and exports are correctly set"). The bundle plane now lives in its OWN runtime-minted NATS account (`TB_BUNDLE_<NAME>`): scripts/materials/artifacts/ledger in its JetStream plane (memory storage; account lifecycle = ephemerality), bundle service+router principals minted there, only crossing into TB_APP = service export/import per trigger (importer keeps the derived `tb.bundle.<n>.<e>` names — surface unchanged, examples/clock untouched). New embednats primitives: `Runtime.MintAccount`, `ExportService`, `ImportService` (operator.go; compile into account claims, ride live pushAccount). This supersedes the base64url artifact-auth gap (bundle buckets unreachable from TB_APP regardless of key encoding) and reuses the app plane's bucket names on purpose. Proofs: `TestBundleAccountSeam` (embednats: no-crossing, imported round trip, JetStream isolation, typed denials), `TestBundle` 13/13 w/ new isolation oracles ($JS.API.> probe: tb_bundle ErrBucketNotFound in app account, projection absent from app tb_material while shell routes serve from bundle store), full battery PASS, live browser across the boundary (boot 08:45:37Z -> trigger -> 08:45:52Z). Evidence: `docs/matched-abstraction/task/bundle-account-isolation.md`. Next: slice 3 `bundle-zip`.

Prior: `bundle-dir-app` — DONE (2026-06-12, bundle-v1 slice 1), then REFINED same day per user decision: **authority is derived, never declared** ("proxy/always-append-prefix — that's the auth setup"; trust posture: operator loading a bundle knows what's inside). Manifest entries are now just name/file/command/projections(short ids)/boot; loader derives `scripts.bundle.<n>.<e>`, `tb.bundle.<n>.<e>`, `bundle.<n>.<id>`, `bundle/<n>/`; perms = single wildcard `tb.bundle.<n>.>`; the whole collision-check family was DELETED (manifest can't spell a collision; free-form fields = unknown-field rejections). Re-driven RED-GREEN, 13/13, full battery, live browser re-proof. Approach invariant 2 amended + Plan Escalation Log entry (includes the base64url constraint: subject perms can't scope artifact-manifest/script-record KV keys → artifact grants stay in-process ScriptPolicy; per-bundle buckets = auth-true evolution if untrusted bundles ever land). Original implementation notes:

- Docs: `docs/matched-abstraction/{approach,plan}/bundle-v1.md`, `task/bundle-dir-app.md` (complete, full evidence).
- Surface: `Config.BundleDir` / `--bundle <dir>`; `loadBundle` (strict manifest decode, disjoint-authority checks vs durable claims + reserved subjects + intra-bundle dups, fail-fast BEFORE NATS starts); `startBundle` (memory-storage `tb_bundle` bucket = ephemerality mechanism, per-entry router route + script loop + ScriptPolicy grants, boot via caller-creds request/reply with per-run request id); shell routes `GET /artifacts/<name>` (recorded mediaType + `CSP: sandbox allow-scripts` + ACAO `*` — sandboxed pages have opaque origins, so their fetches are cross-origin) and `GET /projections/<id>`; embednats `LoadArtifact`/`LoadProjection` read-only getters. Typed kind `BundleRejected`.
- Example: `examples/clock` (manifest + framed-stdio sh script emitting projection + HTML page). Proven LIVE: agent-browser rendered the page, `nats request ... tb.bundle.clock.tick` -> accepted -> page self-updated within its 2s poll.
- `TestBundle` 13/13 over real NATS; full battery PASS (all gates, 9/9 Go packages).
- KEY POSTURE FACT (recorded in task Residual Risk): account identity is ephemeral -> JetStream durable buckets are empty on every boot (fresh plane); the durable-key collision check is vacuous today but kept for when planes persist.
- Run: `go run ./cmd/tinkabot --store /tmp/tb-clock --shell 127.0.0.1:8419 --bundle ../../examples/clock` then open `/artifacts/bundle/clock/index.html`.
- Next when resumed: bundle-v1 slice 2 (`bundle-zip`: extraction front-end + content-hash provenance, per Plan).

Prior brainstorm record (2026-06-12, converged with user, now superseded by the docs above): **bundle** feature — `--bundle <dir|zip>` startup param serving included scripts per manifest. Converged model:

- Bundle = **ephemeral run posture** (user decision): loaded at startup, nothing durable mutated, gone on exit. Records land in a **memory-storage JetStream KV bucket** (still NATS-observable, dies with the process). Loader acts as an automated author (strict-decoded `bundle.manifest`, all-or-nothing, typed attributed failures, fail-fast at boot). Zip = extraction front-end to the dir path.
- **Disjoint authority** (user decision: disjoint namespaces; generalized): a bundle may not claim any authority already claimed durably — scriptKey, wired trigger subject, projectionId, artifact prefix. Collision anywhere = typed load failure. Consequence: the manifest MUST declare its own wiring + grants (bundle scripts can never occupy the boot-fixed `scripts.app.main` slot).
- Effects stay durable; ledger provenance carries bundle name + content hash so history survives the bundle. Revisions manifest-declared, pinned per run; paths rewritten to the bundle dir at load.
- Non-goals: dir watch/live-reload, shadowing/override semantics, session/agent definitions in bundles.
- **One-stroke app** (user direction, 2026-06-12): a bundle = a complete app, backend + frontend together. Frontend needs no new mechanism — in this platform a frontend IS artifacts rendered by the trusted shell's sandbox. Missing piece: a manifest-declared **boot script** fired by a new `bundle-load` activation source (deduped per run), which emits the frontend artifacts + initial projections through the NORMAL materializer gate. Explicitly rejected: loader writing materials directly (second truth path, bypasses the materializer). Trust model untouched: bundle UI is untrusted generated content (sandboxed iframe, command allowlist), never trusted-shell code. Durability: code ephemeral, effects durable — `--store` choice is the demo/prod knob (temp dir = throwaway demo). Payoff: today's `TB_DEMO_SESSION` baked-in demo becomes `tinkabot --bundle examples/observe-demo/`.
- Diagram: https://diashort.apps.quickable.co/d/4ee5ebeb — Next step when resumed: matched-abstraction Approach doc (skill installed at `.claude/skills/matched-abstraction`).

Prior: post-program demo: browser-observes-backend example — DONE (2026-06-12, user-requested "frontend to observe backend", no claude — a simple continuous emitter).

- Binary: `TB_DEMO_SESSION=<id>` (env on `cmd/tinkabot`, `Config.DemoSession`) spawns a demo-gated mediated session: a sh ticker subprocess under the REAL session subsystem emitting canonical token frames 1/sec (`tinkabot/demo_session.go`). Required an operator-mode branch in `StartSessionRuntime` (runner credential now minted through the breadth-checked `MintUser` seam in operator mode; `runnerPerms` is the single shared authority set; non-operator static path unchanged) — sessions had never been spawned in operator mode before.
- Shell: observe panel (`apps/frontend/src/observe.ts` + `nats.ws` dep): mint grant -> ticket-gated WS connect -> deliver-subject subscribe -> streaming render. Proven LIVE with agent-browser: ticks stream continuously; reload replays from tick 1 (DeliverAll snapshot-plus-tail) then continues live.
- EMPIRICAL FINDING (recorded in lessons.md + slice-7 task doc addendum): browsers do not attach SameSite cookies (Strict or Lax) to ws:// upgrades, while the same cookie rides fetch — every Go-side link proof passed, the browser composition failed. Resolution: the cookie-gated mint endpoint also derives a single-use 30s upgrade ticket (`wsTicket`, `embednats.IssueUpgradeTicket`/`RedeemUpgradeTicket`); `GET /session/ws` accepts cookie OR ticket; gating authority unchanged (ticket obtainable only via cookie), posture narrower. Owned by `TestWebSessionShell/TicketGatedUpgrade` (fresh ticket cookieless 101+INFO; reuse 401; forged 401) and `TestDemoSession` (full cookie->mint->bearer-WS->deliver dogfood in Go).
- Run it: `TB_DEMO_SESSION=demo-001 go run ./cmd/tinkabot --store <dir> --shell 127.0.0.1:8419`, open the shell URL, click Observe.

Prior: `web-session-surface` — DONE (session-v2 slice 7/7, program close).

Workflow vetting result (important precedent): the `quality-slice` main run (21 agents) passed its own six subtests but UNDER-DELIVERED the Plan — no WebSocket surface existed anywhere (the "upgrade" proof tested a bare function), the viewer credential granted steer-subject publish (invariant-1 violation), the viewer subscribed `.out` directly (no transcript replay) instead of the deliver-consumer design, no bearer mode (seed shipped browser-side), and a parallel Go `FrameLease` was invented instead of extending the TS lease. Caught by vetting GREEN against a pre-built seam map (binary shell server, WS posture, mint API, TS lease, release closure points) rather than against the workflow's own claims. The slice was re-driven under a strengthened RED.

RED-GREEN-TDD result:

- RED (2026-06-12, strengthened): embednats compile-fail (`MintViewerCredential` ttl arg, `ViewerCred.JWT/DeliverSubject`, `BindViewerDeliver` undefined); tinkabot behavioral fail (no `tb_shell` cookie, WS route 404).
- GREEN hop one: `MintViewerCredential(rt, sid, ttl)` mints a BEARER JWT (`BearerToken=true` re-sign with the account key, Src loopback CIDR pin, leaf scope: subscribe own `tb.session.<id>.deliver.<nonce>` + `_INBOX.>`, publish `tb.app.browser.command` only); `BindViewerDeliver` creates the substrate-bound DeliverAll push consumer over `tb-session-out-<id>` (snapshot-plus-tail); cookie session store in JetStream KV (`IssueSessionCookie`/`ValidateCookieSession`/`RevokeCookieSession` via one `withCookieKV` seam); binary enables the loopback WS listener, issues the HttpOnly SameSite=Strict `tb_shell` cookie, serves `POST /session/viewer` (cookie→bearer grant, revokes the mint if the deliver bind fails) and the cookie-gated `GET /session/ws` proxy that replays only WS handshake headers (cookie never crosses into NATS).
- GREEN hop two: TS lease in `apps/frontend/src/isolation.ts` gains `sessions` scope + `mayObserve`; `accept()` rejects out-of-scope steer intents with typed `FrameScopeEscape` (frontend tests own the family).
- Closure: scenario-matrix `web-session-surface` surface (7 families → committed Go tests across `TestWebSessionSurface`/`TestWebSessionShell`); `release/v1.json` milestone + `session-v2` spine step; `release-evidence.ts` MILESTONES/SPINE grown, `direct-browser-nats-websocket` removed from DEFERRED with a `retiredDeferrals` record (loopback only; external/TLS stays deferred).
- Verified (2026-06-12): `TestWebSessionSurface` 6/6 over real NATS (bearer-over-WS observe with pre-attach replay, denied-neighbor + steer-publish denial output-parsed, expiry/revocation/renewal/cookie-revocation, typed `ViewerMintFailed`, matrix+manifest closure checks); `TestWebSessionShell` 4/4 (HttpOnly cookie, 401 ungated/forged upgrade, 101 + NATS INFO banner through the proxy twice, mint endpoint 401/400/200 + e2e observe over WS + steer landing on `tb.app.browser.command`); frontend 7 pass; full battery all 14 commands + 5 gates + `git diff --check` PASS; one expiry-boundary flake fixed (JWT expiry truncates to seconds — poll-until-denied oracle). Reviews: security pass (bearer revocation binding confirmed; orphan-mint-revoke advisory applied), be-lazy fail→fixed (mayObserve reuse, bool ValidateCookieSession, withCookieKV), no-slop per finisher. Evidence in `docs/matched-abstraction/task/web-session-surface.md`.

Prior slice: `agent-wrapper-proof` — DONE (session-v2 slice 6/7). Go wrapper decision recorded in the Plan Escalation Log; live-claude e2e via `TB_E2E_CLAUDE=1`. Evidence in `docs/matched-abstraction/task/agent-wrapper-proof.md`.

Prior slice: `steering-acceptance` — DONE (session-v2 slice 5/7). Evidence in `docs/matched-abstraction/task/steering-acceptance.md`.

Prior slice: `trusted-wrapper-authority` — DONE (session-v2 slice 4/7). Evidence in `docs/matched-abstraction/task/trusted-wrapper-authority.md`.

Prior slice: `session-frame-mediation` — DONE (session-v2 slice 3/7). Evidence in `docs/matched-abstraction/task/session-frame-mediation.md`.

Prior slice: `session-runtime-subsystem` — DONE (session-v2 slice 2/7). Evidence in `docs/matched-abstraction/task/session-runtime-subsystem.md`.

Prior slice: `session-contract-authority` — DONE (session-v2 slice 1/7). Evidence in `docs/matched-abstraction/task/session-contract-authority.md`.

## Closeout Snapshot

- All sixteen v1 milestones and all five `quality-v1` slices are DONE. The v1 platform target is reached; the next resume point is committing and pushing the five uncommitted quality-v1 slices (quality-gate-infrastructure, typed-exposure-posture, operator-jwt-authority, tinkabot-binary, quality-release), then choosing the next program.
- `bun run release:evidence` over `release/v1.json` is the single passing release gate for both programs: 16 milestones over 11 spine steps plus 5 gate results, deferred scope named, four Plan scope guards enforced, doc authority map recorded, manual-verbatim claim validated.
- No active implementation blocker is recorded. Endgame v1 closeout and the quality-v1 plan are pushed through `bb30c70`; the five quality-v1 slices await commit.
- Do not reopen completed feature slices unless the release gate exposes a concrete unsupported claim or missing proof.

## Milestone Workflow

1. DONE: `endgame-contract-authority`: neutral schemas, fixtures, TS/Zod target, Go validation target, and parity command.
2. DONE: `managed-auth-subjects`: identity/capability provenance, subject taxonomy, NATS auth compilation fixtures, lease/revocation/expiration proof, advanced capability denial, bounded responses, and export/exposure pairing.
3. DONE: `command-acceptance`: durable intent acceptance, atomic idempotency, required command ids, capability context binding, stale-revision denial, capability lease denial, status materialization, activation handoff.
4. DONE: `substrate-edge-bootstrap`: Go substrate boundary plus Browser Edge credential/artifact bootstrap over shared contracts.
5. DONE: `go-substrate-core`: Go-owned embedded NATS lifecycle, HA/scale topology, auth render, credential leases, store substrate, activation ledger, process boundary, gateway substrate, attribution.
6. DONE: `embedded-nats-adapter`: Go substrate contracts attach to a real embedded NATS runtime with JetStream, auth load path, WebSocket posture, topology probes, and drain/shutdown behavior.
7. DONE: `activation-contract-authority`: canonical activation source contracts, fixtures, SDK validation, and Go validation for all source kinds under the activation foundation plan.
8. DONE: `activation-ledger-durability`: durable activation records, source cursors, duplicate resolution, loop suppression, replay/catch-up, and restart behavior.
9. DONE: `activation-source-authority`: source-scoped NATS auth, permissions, imports, exports, bounded responses, revocation, and denied-neighbor proof.
10. DONE: `frontend-isolation-layer`: Vite shell, opaque generated iframe fixture, leased source-window message path, raw-authority denial, and Go-embedded frontend build.
11. DONE: `browser-isolation-proof`: gateway Command Acceptance smoke proof plus service-worker scope/header denial.
12. DONE: `activation-router-live-sources`: request/reply, subject subscriptions, KV/Object/Stream watches, and accepted activation normalization over live NATS.
13. DONE: `activation-schedule-engine`: durable schedule state, lease/leadership, fake-clock tests, catch-up, restart recovery, tick dedupe, and loop safety.
14. DONE: `activation-release-proof`: outside-in real NATS activation scenarios tied back to inside-out contract, ledger, source authority, router, command-acceptance peer evidence, and schedule proof.
15. DONE: `script-materializer-loop`: mediated script execution, accepted effects, materialized projections/artifacts, cleanup.
16. DONE: `release-spine`: centralized ops evidence manifest with outside-in real NATS proof and inside-out ownership proof.
17. DONE: `quality-gate-infrastructure` (quality-v1 slice 1/5): four standing gates (`gate:fakes`, `gate:parallel`, `gate:coverage`, `gate:scenarios`), harness factory seam, fully parallel shuffled corpus, fakes allowlist, coverage floors, scenario matrix, injected-violation detection proof.
18. DONE: `typed-exposure-posture` (quality-v1 slice 2/5): typed exposure posture through the harness seam — in-process default with no TCP endpoint, explicit loopback opt-in carrying the `nats` CLI proofs unchanged, typed denied-by-default external tier, four typed failure families (`ExposureUndeclared`, `ExposureDenied`, `ExposureMismatch`, `InProcessConnFailed`), pre-bind denial of exposure widening, whole corpus on declared postures.
19. DONE: `operator-jwt-authority` (quality-v1 slice 3/5): real embedded NATS in operator/JWT mode through the harness seam — substrate-held operator key with first-start generation and reload, control/app account split, user-JWT minting carrying `core.Capability` lease provenance, live `UpdateAccountClaims` push and supersession, revocation disconnecting live + denying reconnect (closes the deferred live-auth-reload item), six typed failure families (`OperatorKeyFailed`, `AccountCompileFailed`, `JWTMintFailed`, `AccountUpdateFailed`, `RevocationFailed`, `ProvenanceLost`). The manual connection preamble is revised to JWT creds and proven by `TestOperatorCLIRequestWithCreds` (real CLI, minted creds, allowed reply verbatim, denied neighbor output-parsed); KV/Object/publish behavior commands run creds-mode with the slice-4 binary.
20. DONE: `tinkabot-binary` (quality-v1 slice 4/5): the v1 product entry surface — `substrate/go/tinkabot` assembly + thin `cmd/tinkabot` entry point assembling operator/JWT auth, typed exposure, embedded frontend shell, and the script materializer loop into one startable, restartable, CLI-operable binary. First-start materialization (operator key + caller/observer/author creds) vs byte-identical restart reload, shell with proven policy headers, posture==served-surface wiring, drain-by-revocation idempotent shutdown, five typed failure families. `TestBinaryManual` closes the carried creds-mode behavior-commands sweep over the real `nats` CLI (all denial oracles output-parsed); `docs/manual/v1.md` gained "Starting the binary" validated by `CheckManual`.
21. DONE: `quality-release` (quality-v1 slice 5/5, program close): `bun run gate:manual` runs the manual's documented command/outcome pairs verbatim against the running binary (creds mode, output-parsed oracles, live divergence detection); `bun run release:evidence` extended with a hardcoded five-gate list and `release/v1.json` `gateResults`; three new failure families (`gate-result-missing`, `gate-overclaim`, `manual-divergence`) plus gate-result `evidence-stale`, each genuinely detected on synthetic corpora and synthetic negatives over the real corpus; two manual divergences fixed in `tinkabot-binary` surfaces (served wiring literals, lowercase wire statuses); deferred list and scope guards intact.
22. DONE: `session-contract-authority` (session-v2 slice 1/7): canonical session contract in `schemas/base/v1` — `sessionRecord`, `sessionFrame` (out `token`/`chunk`/`status`, in `steer`/`stop`) with const-pinned frame origin making wrapper-emitted status frames contract-rejectable, `steerIntent`, owner-layer-tagged `trustTier` distinct from `AuthTier` and ledger run-claims; TS/Zod and Go parity over nine owner-layer-tagged fixtures via `session.cases.json` through the existing registry, zero new wiring; four owning tests (parity, unknown-frame, missing-provenance, reserved-vocab); rawWords resolved by naming only, facade scan and denial guard unchanged.

## Operating Rules

- Read this file first at session start.
- Use RED-GREEN-TDD for non-trivial changes.
- Use `matched-abstraction` for architecture, planning, task handoff, and layer docs.
- Use `triage-three` before presenting non-trivial concepts or architecture choices; skip for direct execution/status.
- Use `be-lazy` when coding: short names, compiler-backed inference, direct code, explicit only at public/safety/schema/error boundaries.
- Verify before done. Prefer narrow meaningful checks, then record evidence here only when it changes the current handoff.

## Current Direction

- Endgame Approach is the current top-level app authority: `docs/matched-abstraction/approach/endgame-app.md`.
- Endgame Plan is the current decomposition authority: `docs/matched-abstraction/plan/endgame-app.md`.
- Browser Isolation Approach/Plan are the current generated-content isolation authority: `docs/matched-abstraction/approach/browser-isolation.md`, `docs/matched-abstraction/plan/browser-isolation.md`.
- Go Substrate Approach is sealed for `go-substrate-core` and downstream Go substrate planning: `docs/matched-abstraction/approach/go-substrate.md`.
- Activation Foundation Plan is the current activation decomposition authority: `docs/matched-abstraction/plan/activation-foundation.md`.
- Script NATS CLI Proof Plan is the current script-side outside-in proof authority: `docs/matched-abstraction/plan/script-nats-cli-proof.md`.
- The product loop is source/artifact -> materialized projection -> browser intent -> durable backend acceptance -> activation -> script execution -> attributed event/projection update.
- Go owns substrate authority: embedded NATS lifecycle, NATS-native HA/scale posture, auth, process lifecycle, Docker/sandboxing direction, connection policy, activation ledger, artifact gateway, execution attribution.
- Vite owns the trusted browser shell. Generated browser content remains an opaque sandboxed receiver and intent emitter.
- Schema/SDK owns shared contract shape. JSON Schema is the first neutral source; generated or checked Zod, TS types, Go validators/types, and fixtures follow it.
- Existing Bun/TypeScript runtime and `@lagz0ne/nats-embedded` work is regression evidence and SDK material, not current substrate authority.
- NATS is the system seam. Inside-out tests localize ownership; outside-in tests must prove cross-lane behavior through real NATS-mediated surfaces before a release-shaped slice counts as release-ready.
- Default scripts stay NATS-agnostic process contracts. Runtime facade mediates NATS publish/progress/import requests.
- Script-side outside-in proof uses real `nats` CLI commands against embedded NATS to trigger behavior and observe replies, statuses, streams, KV/Object Store records, or materialized projections.
- Activation is a first-class layer above substrate; request/reply is only one activation source.
- Browser edge owns session bootstrap, service-worker setup, browser credential mint/revoke, artifact serving, cache/CSP/sandbox policy, and missing browser control-plane behavior.
- Service-worker setup is substrate-owned: the server issues an HttpOnly/Secure/SameSite cookie session, serves the worker script under a controlled scope, and exposes a scoped substrate surface without handing generated content tokens, NATS credentials, subjects, permissions, cookies, or registration authority.
- Control plane and app plane are separate authority domains.
- After `release-spine`, the next program is `quality-v1`: deliver a usable, high-quality v1 with four enforced gates — all tests over real embedded NATS with an explicit fakes allowlist, parallel test execution with isolated servers, dual coverage (inside-out per-layer measurement plus outside-in scenario-matrix completeness), and `be-lazy` style enforced by a diff-scoped reviewer gate per slice.
- The v1 user entry surface is a single Go binary: embedded NATS plus embedded frontend shell plus the script materializer loop, operated through the `nats` CLI. Product UI rendering stays deferred.

## Next Program: Session V2

The next program is decided: `session-v2` — long-lived agent sessions. A wrapper process (driving an agent runner such as `claude` over structured stdio; Go, per the 2026-06-12 escalation) stays alive, streams output continuously, and accepts steering mid-run; the substrate exposes the session over NATS for observation and steering; a trusted browser surface streams content and sends steering chat.

Matched-abstraction docs are authored:
- Approach: `docs/matched-abstraction/approach/session-v2.md` (purpose, core thesis, eight invariants, non-goals, decision hierarchy, met Plan-readiness gate).
- Plan: `docs/matched-abstraction/plan/session-v2.md` (seven-slice decomposition, dependency ordering, handoff contract, verification strategy, deferred scope).
- Diagram (authority + recovery model): `https://diashort.apps.quickable.co/d/13b0196d`.

### Next Step

ALL SEVEN session-v2 slices are DONE and the program-level final review was executed (2026-06-12, two adversarial per-invariant evidence sweeps + Fable spot-checks). Awaiting the user's judgment on the findings below; the recommended follow-up is one small slice (working name `steer-apply-gate`) closing the two primitive-only gaps.

Final review verdicts (vs the eight Approach invariants):
- HOLDS: 2 (control-plane trust), 4 (single mediated output writer, NATS-enforced via BridgeBypassAttempt), 7 (lossless attributed steering — all four slice-5 fixes proven).
- HOLDS-WITH-CAVEAT: 3 (leaf scope — breadth check covers all operator-mode mints; `sessionCred`'s non-operator `addSessionUser` path bypasses it but is unreachable in the binary), 5 (subsystem separation is structural; no concurrent-load starvation proof), 8 (browser surface proven link-wise; `Secure` cookie flag deferred with TLS; hop-two enforcement is TS-layer only).
- GAP (primitive exists + tested, not wired into production): invariant 1's apply-time revocation re-check — `ApplySteerAfterRevoke` (operator.go) is called ONLY from tests; the physical steer pipe (`session_runtime.go:260` and the trusted wrapper) writes to stdin unconditionally. Carried from slices 4/5 composition: no production component composes accepted-steer-activation -> lease re-check -> wrapper stdin (also: nobody in production publishes the steer subject; single-writer holds by absence of grants). Invariant 6's restart reconciliation sweep is likewise primitive-only — `ReconcileOrphanedSession` has zero production call sites, correctly bounded by the fact that the binary does not yet spawn sessions (session CRUD is deferred scope); the sweep belongs to whatever lands session spawning.

Resolved carry-notes (for the record):
- Sessions are now a binary surface at the observation level: cookie session, viewer mint endpoint, cookie-gated WS proxy, loopback WS listener. The wrapper-side manual pairs still run under the proof harness (the wrapper itself is an external process by design).
- The body-`sessionId` integrity gap is resolved by design: viewers trust subject/stream identity (the deliver consumer binds per session stream); the body field stays unvalidated data. Recorded in the slice-7 task doc.
- Live-agent flag set (`--verbose` mandatory) and the `TB_E2E_CLAUDE=1` guard precedent stand.

Residual risks carried out of the program (named in the slice-7 task doc): browser composition proven link-wise in Go, not as one browser run (the shell UI stays deferred with product UI rendering); `ValidateCookieSession` opens a connection per check (cache seam if traffic ever grows); hijacked WS proxy conns end with either side, bounded by process exit.

Operating pattern learned this program (keep using it):
- Run each slice via `Workflow {name: quality-slice, topic: <slice>}` (agents pinned to sonnet, commit `c43948d`). The main run usually completes RED+GREEN and 4-5 gates; no-slop/be-lazy often stall on comment-narration cosmetics after 3 fix rounds.
- Pattern: apply remaining gate blockers manually (mechanical comment/name fixes), re-run targeted Go tests, then run a small finisher workflow (re-check failed gates with "senior reviewer would insist" charter + full 16-command battery + wrap-up agent), then ONE atomic commit per slice.
- Watch for substantive findings among cosmetics — caught so far: primary-user authority widening (slice 3, fixed with dedicated minted test creds), FIFO-breaking goroutine send (slice 5, fixed with blocking send), gate-scenarios weakening (slice 5, reverted — gates must never be loosened to fit a slice; fold extra families into the seven pinned ones).
- Workflow resume replays cached results (including failed verdicts) — don't resume to re-run gates; write a fresh finisher script instead.
- Gate agents must be told to return the exact gate key (a `security/authority` name mismatch once dropped a verdict silently).

After slice 7 (includes release closure: manifest + gates extension covering the session program, plus the direct-browser-WebSocket deferral retirement record), do the program-level final review the user reserved ("we'll judge the result by the end"): full-program diff review against the eight Approach invariants, then present commits, gate evidence, and residual risks for the user's judgment.

Slices in order: `session-contract-authority` (DONE, 8013d49) -> `session-runtime-subsystem` (DONE, fc8effd) -> `session-frame-mediation` (DONE, 526d290) -> `trusted-wrapper-authority` (DONE, cc805f1) -> `steering-acceptance` (DONE, 7aceeaa) -> `agent-wrapper-proof` (DONE, 9ed17d1) -> `web-session-surface` (DONE, 6f17ddc; release closure landed: 17 milestones / 12 spine steps, direct-browser-WS deferral retired at loopback). PROGRAM COMPLETE; final review executed, findings above await the user's judgment.

Every session-v2 slice's owning Plan is `docs/matched-abstraction/plan/session-v2.md`; each wrap-up must keep this pointer and the next slice's topic in this Next Slice section.

## Current Verification Commands

- `bun test` or `bun run test` -> SDK tests.
- `bun run test:e2e` -> SDK distribution BDD.
- `bun run typecheck` -> `bunx @typescript/native-preview --noEmit`.
- `bun run build` -> builds frontend into `substrate/go/frontend/site` and SDK into `packages/sdk/dist`.
- `bun run pack:dry` -> dry package check.
- `bun run orchestrate:codex -- --dry-run --allow-dirty` -> smoke-test the Codex endgame orchestration plan without launching agents.
- `bun run release:evidence` -> centralized release gate over `release/v1.json` (16 milestones, 11 spine steps, 5 gate results, manual-verbatim claim).
- `bun run gate:fakes` / `gate:parallel` / `gate:coverage` / `gate:scenarios` / `gate:manual` -> standing quality-v1 gates; all five must stay green per slice. `gate:parallel` runs the full shuffled Go suite; `gate:manual` runs the manual's documented command/outcome pairs verbatim against the running binary.

## Pinned Decisions

- NATS auth vocabulary is authoritative: `permissions.publish`, `permissions.subscribe`, `allow`, `deny`, `allow_responses`.
- Metadata uses `desc`, not `meaning`.
- Model both access and exposure: inside-out imports/publish and outside-in activation/consumption.
- Subjects must be concrete values or concrete wildcard patterns. No placeholder subject strings.
- Deny wins over allow. `allow_responses` is bounded to invocation/reply.
- Canonical process IPC is framed stdio RPC; fd-specific helpers are adapters only.
- A first slice may be small, but must be complete at its boundary with denial/failure paths.
- NATS auth is the compiled enforcement shape; identity, ownership, session, revision, and capability provenance must survive into it.
- Browser and script credentials are scoped leases, not durable ambient credentials.
- Schema validates shape; capability policy authorizes effects.
- Managed auth compilation denies raw/advanced imports and non-request-reply exposure by default, requires `allow_responses.expiresMs` when response authority is present, distinguishes revoked from expired leases, and requires exported subjects to match declared exposure subjects.
- Command acceptance requires command ids, claims statuses atomically before activation handoff is returned, resolves duplicate command ids without second activation, binds command session/capability context to the active lease, rejects stale revisions, exhausted chain budgets, and revoked/expired capability contexts, and emits `activation.intent` with `source.kind = "command_acceptance"` only for accepted first-seen commands.
- Substrate edge bootstrap stays pure/fakeable: Go derives scoped worker credential descriptors and artifact gateway policy from canonical contracts; Browser Edge splits worker-only credentials from content-safe context and emits only canonical `browser.command_intent` outward.
- Browser service-worker setup belongs to the substrate/browser edge, not generated content. It is cookie-session-backed, scope-isolated, revocable, CSRF/origin checked, and token-free for generated content.
- Browser generated content runs as opaque sandboxed receiver code for v1. The shell must bind IPC by source window or port, nonce, frame lease, schema revision, artifact revision, and capability context; `event.origin` alone is not authority.
- Unsafe same-origin `allow-scripts` plus `allow-same-origin` is denied for untrusted generated content.
- Direct browser NATS WebSocket is deferred until live credential reload, post-connection revocation, denied-neighbor, stale-access, and confidentiality proof exist. Update (session-v2): the first four are proven by `operator-jwt-authority`; confidentiality is satisfied at the loopback posture. The deferral is retired at loopback by `docs/matched-abstraction/approach/session-v2.md` invariant 8 (two-step mint, cookie-gated WS upgrade, leaf-scoped bearer viewer cred, no JetStream API authority in the browser). External/TLS forms stay deferred.
- Go substrate Approach is sealed for `go-substrate-core`; downstream Plan/Task work may refine decomposition and verification, but cannot redefine embedded NATS ownership, HA/scale authority, auth vocabulary, authority envelopes, mediated scripts, generated-content denial, or typed substrate failures.
- Go substrate embeds and manages NATS by default; HA/scale posture uses NATS-provided clustering, JetStream replica/quorum, route/gateway/leaf, WebSocket, queue/consumer, and observability semantics rather than bespoke substrate replication or routing.
- Go substrate core must exist before embedded-NATS adapter, activation, script, or materializer implementation consumes substrate behavior; TypeScript runtime work is regression evidence, not substrate authority.
- Go substrate core is complete as a pure/fakeable contract package under `substrate/go/core`; embedded-NATS adapter consumes it rather than redefining substrate contracts.
- Embedded NATS adapter must sit between Go substrate core and activation foundation so real NATS lifecycle, JetStream, auth load path, WebSocket posture, topology probes, and drain/shutdown semantics are proven before reactive triggers depend on them.
- Embedded NATS adapter is complete under `substrate/go/embednats`; it proves a live single-node embedded server, JetStream `AccountInfo` readiness, NATS auth user loading, least-authority internal probe credential, router-safe `Runtime.Connect`, WebSocket random URL posture, topology probe failure, drain wait, shutdown timeout, stop panic recovery, and adapter-owned error mapping.
- Activation foundation must sit after embedded-NATS adapter and before script-materializer-loop so reactive triggers do not get invented inside script execution.
- Activation contract authority comes before live source routing. The live router consumes canonical source shape, source principal, source lease, cursor, wildcard, provenance, chain, dedupe, and parity fixtures.
- Activation contract authority and durable ledger behavior are complete. Source-scoped authority is now the next activation foundation task.
- Activation foundation is one program with task-owned proofs: contract authority, ledger durability, source authority, live router, schedule engine, and release proof.
- Activation ledger durability stays below source authority and live routing: it records accepted attempts, source cursor state, duplicate/loop/replay outcomes, lease binding, and durable failures, but it does not decide whether a source principal may observe a subject, bucket, object, stream, or schedule. Durable proofs should use embedded NATS/JetStream where available; mocks/fakes are only for narrow branch forcing.
- Activation source authority is complete below live routing: it authorizes source observation with NATS-shaped permissions/imports/exports/exposure, source lease lifecycle/revision checks, bounded request/reply responses, denied-neighbor checks, and typed attribution. Ordinary `subject` sources currently reject `>` apertures as overreach; bounded `*` aperture is allowed.
- Live source router is complete below schedule activation: it turns real embedded-NATS request/reply, subject, KV, Object Store meta-stream, and stream observations into source-authorized durable activation records. Object Store routing uses the meta stream instead of `ObjectStore.Watch()` so the source position preserves JetStream sequence metadata.
- Schedule engine is complete below activation release proof: schedule source position now uses deterministic clock position rather than leader epoch; leader epoch and fencing remain authority identity, while clock position is tick progress. Embedded NATS KV stores schedule state for restart catch-up.
- Activation release proof is complete below script-materializer-loop: live router source kinds are release-proven through real embedded NATS, command acceptance remains peer-owned by Browser Isolation Proof, schedule is proven over NATS-backed durable stores rather than a NATS tick facade, and failure attribution is normalized only inside tests.
- Script-side outside-in proof is driven by real `nats` CLI commands against embedded NATS. CLI proves caller/platform behavior; it does not give default scripts raw NATS authority.
- Release gates must include allowed, denied-neighbor, malformed, duplicate, stale-revision, revoked-credential, and attributed-failure cases over NATS-mediated behavior.
- `bun run release:evidence` over `release/v1.json` is the single v1 release gate. The checker hardcodes the Plan gate list (sixteen milestones, eleven spine steps, eight deferred items, four scope guards, seven pinned case families) so the manifest cannot weaken its own gates; negative-case citations must name their case inside executed verification evidence, and denial oracles must be output-parsed, never exit-code.
- Post-closeout rename, "endgame" -> "base"/"v1" on live surfaces only: `tb.schema.base.*` wire ids, `schemas/base/v1`, `packages/sdk/{src,tests}/base-contract`, `release/v1.json`, and the `quality-slice` workflow. Historical doc evidence and milestone names keep the original "endgame" wording verbatim; executed commands recorded before the rename are not re-runnable at their old paths by design.

## Milestone Index

Historical details live in matched-abstraction docs and git history. Do not expand this handoff with completed evidence unless it changes current work.

- NATS runtime design: `docs/matched-abstraction/{approach,plan}/nats-script-runtime.md`.
- Traced TDD plan: `docs/matched-abstraction/plan/nats-script-runtime-traced-tdd.md`.
- Runtime substrate and record store proof: `docs/matched-abstraction/task/nats-script-runtime-substrate-record-store.md`.
- Distribution BDD proof: `docs/matched-abstraction/task/nats-script-runtime-distribution-bdd.md`.
- Metadata and permissions proof: `docs/matched-abstraction/task/nats-script-runtime-metadata-permissions.md`.
- Activation contract proof: `docs/matched-abstraction/task/nats-script-runtime-activation-contract.md`.
- Request/reply activation adapter proof: `docs/matched-abstraction/task/nats-script-runtime-request-reply-activation-adapter.md`.
- Browser frontend mediator proof: `docs/matched-abstraction/task/browser-frontend-dedicated-worker.md`.
- Browser isolation approach: `docs/matched-abstraction/approach/browser-isolation.md`.
- Browser isolation plan: `docs/matched-abstraction/plan/browser-isolation.md`.
- Browser isolation proof task: `docs/matched-abstraction/task/browser-isolation-proof.md`.
- Frontend isolation layer task: `docs/matched-abstraction/task/frontend-isolation-layer.md`.
- Platform reset: `docs/matched-abstraction/{approach,plan,task}/platform-structure*.md`.
- Code structure reset: `docs/matched-abstraction/{approach,plan}/code-structure.md` and `docs/matched-abstraction/task/code-structure-reorganization.md`.
- Endgame app approach: `docs/matched-abstraction/approach/endgame-app.md`.
- Endgame app plan: `docs/matched-abstraction/plan/endgame-app.md`.
- Go substrate approach: `docs/matched-abstraction/approach/go-substrate.md`.
- Go substrate plan: `docs/matched-abstraction/plan/go-substrate.md`.
- Activation foundation plan: `docs/matched-abstraction/plan/activation-foundation.md`.
- Endgame contract authority task: `docs/matched-abstraction/task/endgame-contract-authority.md`.
- Managed auth subjects task: `docs/matched-abstraction/task/managed-auth-subjects.md`.
- Command acceptance task: `docs/matched-abstraction/task/command-acceptance.md`.
- Substrate edge bootstrap task: `docs/matched-abstraction/task/substrate-edge-bootstrap.md`.
- Go substrate core task: `docs/matched-abstraction/task/go-substrate-core.md`.
- Embedded NATS adapter task: `docs/matched-abstraction/task/embedded-nats-adapter.md`.
- Activation contract authority task: `docs/matched-abstraction/task/activation-contract-authority.md`.
- Activation ledger durability task: `docs/matched-abstraction/task/activation-ledger-durability.md`.
- Activation source authority task: `docs/matched-abstraction/task/activation-source-authority.md`.
- Script NATS CLI proof plan: `docs/matched-abstraction/plan/script-nats-cli-proof.md`.
- Codex endgame orchestration plan: `docs/matched-abstraction/plan/codex-endgame-orchestration.md`.
- Codex endgame orchestrator task: `docs/matched-abstraction/task/codex-endgame-orchestrator.md`.
- Release spine task: `docs/matched-abstraction/task/release-spine.md`.
- Quality gate infrastructure task: `docs/matched-abstraction/task/quality-gate-infrastructure.md`.
- Typed exposure posture task: `docs/matched-abstraction/task/typed-exposure-posture.md`.
- Operator JWT authority task: `docs/matched-abstraction/task/operator-jwt-authority.md`.
- Tinkabot binary task: `docs/matched-abstraction/task/tinkabot-binary.md`.
- Quality release task: `docs/matched-abstraction/task/quality-release.md`.
- Session contract authority task: `docs/matched-abstraction/task/session-contract-authority.md`.

## Recent Git

- `99cc3c1 chore: establish tinkabot workspace baseline`.
- `42d44fe chore: record git baseline`.
- `5c30a1f chore: add terse coding skill`.
- `f93b705 feat: add script materializer loop`.

## Cleanup Note

This file was reduced from a completed-evidence log to a current handoff. Completed details belong in layer docs, tests, and git commits.
